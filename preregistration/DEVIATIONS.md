# V2 Deviations & Interpretations Log

**Integrity anchor (external, by design).** This file's SHA-256 is committed to the **public** repo at `preregistration/DEVIATIONS.sha256` **before the sweep is run**. That commit is the third-party timestamp; this file is therefore tamper-evident and provably dated without disclosing its contents. The log itself stays **local and gitignored** — it quotes the embargoed pre-registration and cannot be published until the OSF embargo lifts, at which point the pre-committed hash will verify it unaltered. *No commit hash for THIS version's own anchor is recorded inside this file — doing so would change the file and invalidate its own anchor; the anchor lives in the repo, not in the log. Prior, already-immutable commit hashes ARE cited where disclosure requires it (see the Anchor history and C-1).*

**Analysis code anchor.** The finalized analysis code is committed to the repo **before the real-engine sweep runs** — git itself provides the hash and timestamp, so nothing needs recording here. Discipline: the code is finalized and debugged on the **RANDOM control arm** (where the expected answer is chance), never on the real arm, so that no implementation choice is made while looking at the result of interest. The real-engine sweep is then run **once**.

**Anchor history.**
- **Version 1** — entries D-1, I-1, I-2, I-3, I-4, V-1 (D-2 was subsequently added to the body under this stale header). Committed at public commit `73e65f9`, but `preregistration/DEVIATIONS.sha256` there carried the template placeholder `<PASTE_HASH_HERE>` instead of a computed SHA-256. **Version 1 was therefore NOT actually hash-anchored** — `73e65f9` establishes a date only and provides no tamper-evidence for this file's contents. Disclosed, not repaired; the commit is left unaltered. See **C-1**.
- **Version 2** — 2026-07-09 (superseded by Version 3). Entries: D-1, D-2 (+ ratification addendum), I-1, I-2, I-3, I-4, V-1, C-1, paper-framing notes. First **real** content hash of this log; committed before the sweep and before any decision quantity.
- **Version 3** — 2026-07-17. Adds **S2-CENSUS** (Study 2 clean-control viability gate; label-side frontier accounting only). Recorded AFTER the Study 2 registration was hash-anchored (commit `58721ac`), with its own real content hash committed alongside `preregistration/STUDY2_CENSUS.sha256`. Still **no §11 decision quantity** computed (`decision.RATIFIED = False`); the Study-1 sweep remains unrun and Study-2 execution is a later, separate session.

- **Version 4** — 2026-07-17. Adds the ratified **Study-2 execution interpretations** (I-5(rev), I-5a, I-5b, I-6, I-7, I-8), logged **before the Study-2 pipeline code freeze**, plus the **V-1 sign-convention table** (Phase 2, anchored at `preregistration/SIGN_TABLE.sha256`). The Phase-1 pipeline build (random-arm-only debug) is **not yet complete**, so **no S2-EXEC entry, no code anchor C\*, and no random-arm run-log** are recorded here — those belong to a later version once the pipeline is built and frozen. **Phase 3 (real-engine sweep) NOT entered**; `decision.RATIFIED = False`.

- **Version 5** — 2026-07-17. Adds **I-9** (verdict route ordering / C4 dominance lattice, pinned by VERDICT_SUITE 12/12), **I-10** (Phase 2.5 label-side control-arm pre-census, cap-and-run routing), **I-11** (no-relitigation guard). Still **no real-engine indicator×label quantity**; C\* held; `decision.RATIFIED = False`; Phase 3 closed.

- **Version 6** — 2026-07-17. Adds **S2-EXEC** (Study-2 execution-tree freeze): regenerated code anchor **C\*** (`CODE_ANCHOR.sha256` rollup `423f6591…`) over the final tree incl. the Phase 2.5 pre-census (build-only) and Phase 3 block orchestrator (RATIFIED-guarded). Still **no real-engine indicator×label quantity**; `decision.RATIFIED = False`; Phase 3 not entered.

- **Version 7** — 2026-07-17. Hardening (no verdict change): `block_orchestrator.run_block` now **requires** the anchored Phase-2.5 pre-census cap artifact (verifies its SHA-256, refuses if absent/mismatched, derives the route cap from it — making "cap anchored before block" enforced, not operational) and correctly permits upstream gate routes under a cap. C\* moves `423f6591…` → `ce76b3de…`; `decision.py` (verdict logic) unchanged; verdict suite still 12/12. `decision.RATIFIED = False`; Phase 3 not entered.

- **Version 8** — 2026-07-17. Bug fix before the CP-2-authorized pre-census run (no logic change): corrected a mis-leveled relative import inside `pre_census.run()` (`from ...` → `from ..` for `compute_telemetry` / `build_user_episode_manifest`) that raised `ImportError` on the first run attempt (which produced **no output** — pure import error). Both control arms then smoke-tested end-to-end (popularity + shuffled-TS CF path). C\* re-anchored `ce76b3de…` → `36e255cd…`; `decision.py` unchanged; verdict suite still 12/12. Code frozen before the label-side pre-census run. `decision.RATIFIED = False`; Phase 3 not entered.

- **Version 9** — 2026-07-17. Adds **S2-PRECENSUS**: the Phase 2.5 label-side pre-census RAN (CP-2 authorized; artifact `28a73b3a…`, anchored `PRE_CENSUS.sha256` rollup `c8f110a5…`). All six dispositive arms/seeds cleared the I-5a label-side floor → **route_cap = UNCONSTRAINED**. Label-side only; no indicator/S/ΔTPR read; `decision.RATIFIED = False`; the Phase 3 block was not run.

- **Version 10** — 2026-07-18. Adds **I-12** (FAMILY_PARAM_FLAG resolved: sweep each family's v1 structural grid + continuous threshold, matching v1; best baseline = max over {six families ∪ PC1}; anti-SURVIVES direction). A pre-reg decision fixed **blind, before the Phase-3 producer is built**. C\* unchanged (producer still a stub); `decision.RATIFIED = False`; Phase 3 not entered.

- **Version 11** — 2026-07-18. Phase-3 **producer built** (under I-12) + **synthetically validated** (planted→ΔTPR>0.05 w/ correct signs + incr AUC>0.02; permuted→chance) — NEVER run on the real or real-control panels. Generated the **six control-arm indicator-side slate panels** as data prep (slates only; no ΔTPR/separation), anchored `ARM_PANELS.sha256` (`b30ac8d0…`). C\* re-anchored `36e255cd…` → `48c1c3af…` over producer + panels + tests. `block_orchestrator.compute_summaries` wired + RATIFIED-guarded. **No real-engine ΔTPR/AUC/criteria computed**; `decision.RATIFIED = False`; the blind block was **not** run.

- **Version 12** — 2026-07-18. Pre-block hardening (no statistic/definition change): (a) **precomputed the §9 covariates** (`log_activity` + `pop_affinity` LOUO raw re-ingest) on the 25,020 real L2 units — sanity-checked (count matches, no NaN/inf, ranges OK), cached, anchored `COVARIATES.sha256` (`17037515…`); `compute_summaries` refactored to READ the cache (execution-only, I-1/I-2 unchanged). (b) **Block runtime estimated on synthetic ≈ 1 h** (not prohibitive); the score-precompute→index-resample optimization is already implemented, so no statistic changed. C\* re-anchored `48c1c3af…` → `096355fe…`; verdict suite 12/12; producer tests pass. **No real-engine decision quantity computed**; `decision.RATIFIED = False`; block not run.

- **Version 13** — 2026-07-18. **S2-VERDICT**: the Phase-3 blind block ran once under real CP-2 → **UNINTERPRETABLE-BY-CONSTRUCTION** (PC1 gate fails, I-7). No-peek held; gate hard-stop → decision quantities never written. Verdict record anchored `S2_VERDICT.sha256` (`fe6af9ed…`). This is the first entry recording a REAL-engine run; it is a §7 construction gate outcome (not a §11 criteria verdict), so no ΔTPR/AUC/four-criteria value was written or viewed. `decision.RATIFIED` was set in-process for the single run; the anchored source stays False.

- **Version 49** — 2026-07-22. **D-45** (algebra over anchored values, D-43 class — no data accessed): §5.1 cancellation arithmetic — marginals (−0.64)+(0.05)−(−0.68)=+0.09 over composite SD √(3+2[−0.478−0.833+0.415])=√1.208=1.10 → net standardized separation ≈0.08 (the honest effect-size-cancellation reconciliation replacing withdrawn D-43); VIFs from anchored cal-clean matrix ≈3.5 (G), 3.3 (δ), 1.3 (p). Struck rationale: the 'collinearity at 0.975 forces near-identical r(G,p)/r(p,δ)' explanation is disavowed (near-identity was the contamination artifact; cal-clean values −0.478/−0.415 are not near-identical); log search confirms it was never recorded. `decision.RATIFIED = False`.
- **Version 48** — 2026-07-22. **D-44 RESULT** (computed after v47): on 694 cal-fold CLEAN controls r(G,δ)=**0.833** (not the eval-arm 0.975); Var(D)=0.334, Var(S)=1.207, occupancy share **82.8%**. Pre-committed reading **REVERT** (r(G,δ)<0.90): §5.1 reverts to qualitative permanently, D-43 NOT reinstated; occupancy still dominant (~83%) but near-total-cancellation claim withdrawn. Verdict untouched. `decision.RATIFIED = False`.
- **Version 47** — 2026-07-22. **D-44** (RULING + REMEDY, logged BEFORE any value): the Phase-4 scope's 'ON CALIBRATION-FOLD CLEAN CONTROLS' SPECIFIES the population; the executed full-eval-arm correlation (n=12,606) is a D-7-class conformance deviation, so recomputation on the 694 cal-fold CLEAN controls is a REMEDY (permitted), not a D-33 proposal. §5.1 decomposition (D-43) withdrawn pending recompute. Control-only, no label, cannot bear on verdict. Pre-committed reading: r(G,δ)≥0.90 → restore decomposition + reinstate D-43 with corrected figures; r(G,δ)<0.90 → §5.1 reverts to qualitative permanently. `decision.RATIFIED = False`.
- **Version 46** — 2026-07-22. **D-43** (ANALYTICAL DECOMPOSITION at revision, ruled permissible under D-35): §5.1 variance decomposition S=D+z(p), D=z(G)−z(δ); Var(D)=2(1−0.9751)=0.0498, Cov(D,z(p))=−0.8694−(−0.8689)=−0.0005, Var(S)=1.0488, occupancy ≈95% of variance. Algebra over the anchored item-4 correlation matrix; no new empirical quantity, no data access. Logged so the decomposition's provenance (derived at revision, from anchored inputs) is explicit. `decision.RATIFIED = False`.
- **Version 45** — 2026-07-22. **D-42 RESULT** (computed after the reading was anchored at v44): starved-at-≤20 vs clean log_activity — medians 2.890 vs 3.045; Cohen's d (starved−clean) = −0.441 [−0.500, −0.381] (10k, seed 201); **ASSOCIATED FIRED** (direction ✓, |d|≥0.2 ✓, CI excludes 0 ✓). Licensed reading: users with thin early positive history over-represented in the starved stratum; foreclosed readings + non-inference clause bind. Anchored in `s2_exec2_starved_history.json` (S2_PUBLICATION). `decision.RATIFIED = False`.
- **Version 44** — 2026-07-22. **D-42** (PERMITTED CHARACTERIZATION under D-33, logged BEFORE any number): descriptive comparison of I-1 `log_activity`=log(1+warm_start_positive_count) between 3,292 starved-at-≤20 and 1,460 clean controls. Semantic precision binding: WARM-START POSITIVE COUNT = 'thin early positive history', NOT total volume; phrase 'rarely used the service' FORBIDDEN. Pre-registered direction (starved BELOW clean); form = median/IQR + standardized log-scale d with 10k bootstrap CI seed 201 + ECDFs; ASSOCIATED iff direction AND |d|≥0.2 AND CI excludes 0. Foreclosed readings (a)/(b) fixed. Non-inference clause: pop_affinity deliberately NOT compared. Source = C* cache b7a0aa36 (25,020-row superset); set-hash assert vs census 6b6ac55 or halt. `decision.RATIFIED = False`.
- **Version 43** — 2026-07-22. **D-41** (item-6 label correction, logged BEFORE computing probe (b)): two DISTINCT control populations, not one — (a) H3-as-frozen = STARVED-in-(20,50] (3 units) vs CLEAN (1,460), VACUOUS, no discrimination; (b) starvation-concurrence probe = STARVED-at-≤20 (3,292 pre-landmark) vs CLEAN (1,460), substantive. Partition 3,292+1,460+3=4,755 must close. Pre-committed reading for (b): |d|≥0.8 on slate indicators ⇒ slate structure reads frontier depletion (the purified-away contamination). Labeling/reporting split, no new computation. `decision.RATIFIED = False`.
- **Version 42** — 2026-07-22. **D-40** (Phase-4 execution COMPLETE, post-verdict DIES route): six REQUIRED items (D-33) + not-run ledger anchored two-tier as `S2_PUBLICATION.sha256` (rollup `f640d37d…`). t=20 known-answer arm reproduces the sealed payload exactly; PC1 gate PASSED on CLEAN (0.561); S near-chance/strictly dominated (ΔTPR<0 everywhere); bounding diagnostic shows slate discrimination is contamination (d_purified≈0/reversed) while miss_run_fraction retains (+0.81); t_split robust null at 15/20/25; I-5b S beats chance on 0/6 arms; within-window starvers=3,292 (all controls). `decision.RATIFIED = False`; C*** `42075400…` unchanged.
- **Version 41** — 2026-07-21. **D-38** (t_split=15 covariate coverage, pre-committed BEFORE any value: t=25 runs FULL form (subset of t=20, fully cache-covered); t=15 runs PARTIAL form — c1/c2/gates full, c3 COVERAGE-LIMITED with covered/uncovered event counts published; cache NOT extended, no population adjusted — D-35/Route B). **D-39** (Phase-4 harness mechanics disclosed before the run: post-verdict authorship; RATIFIED in-process only, source stays False, C*** unchanged; item-2 contained+restored monkeypatch; item-3 placeholder defect found/corrected pre-run; item-2 t=20 known-answer arm asserts reproduction of the sealed payload, mismatch HALTS). `decision.RATIFIED = False`.
- **Version 40** — 2026-07-21. **D-37** (t_split sensitivity power handling, pre-committed BEFORE the complete-window counts are known: if the eval-fold EVENT count at t_split=15 or 25 < §11 floor of 200, that landmark is reported NOT EXECUTABLE with the count published and the reason stated; no window/filter/floor/admission adjustment to force it). **Footer §4 relabel** (D-27 logic): the 'Verified conformance' whole-L figures (L=25,020; round(b·2,453)={25,49,123,245}) marked explicitly as §4 whole-L accounting, NOT the registered §6 operating points (766 eval CLEAN; {15,38,77,153}); prior entries' substance untouched. `decision.RATIFIED = False`.
- **Version 39** — 2026-07-21. **D-36** (intent to unseal): the seal protected pre-verdict corridor integrity; the verdict is anchored/immutable and §12 commands publication of the full pre-registered analysis, so the quantities open. Phase-4 scope (D-33) and bounding rationale (D-34) were anchored before any value was visible (v38). §4.3 re-cited: a ΔTPR near-but-below 0.05 is a null; Route B (D-35) governs — no quantity outside the pre-registered set. `decision.RATIFIED = False`.
- **Version 38** — 2026-07-21. Post-verdict ruling set (logged BEFORE unsealing, scope fixed blind): **D-32** (thesis correction — withdraw 'post-purification unlearnability'; corrected construct claim; scoped §11; negative's claim ceiling); **D-33** (Phase-4 scope CLOSED — six REQUIRED items, three MOOT with reasons; anything else defaults denied); **D-34** (bounding-diagnostic rationale correction — d_purified descriptive, cannot outrank the registered DIES); **D-35** (post-verdict compute universe = the pre-registered set only; Route B forbids all re-analysis). `decision.RATIFIED = False`.
- **Version 37** — 2026-07-21. **S2-EXEC-2 VERDICT**: the registered analysis executed once under C*** (`run_block_execution2.py`) — input/population/cap assertions passed, block reached a verdict. **VERDICT = SIGNAL DIES**; reason `fails on own merits: c1=c2=c3=False`. Emission held ({verdict,reason,payload_sha256} only); decision_quantities sealed/embargoed, not inspected. Payload file `639b970c`, internal `472571147d94`, `S2_VERDICT` rollup `f1f26e74` (commit `cb7d954`, re-verified). Supersedes vacated exec-1 (D-9). `decision.RATIFIED = False` in anchored source (in-process flip only).
- **Version 36** — 2026-07-21. **D-31**: execution-2 corridor runner `run_block_execution2.py` (sha `fe07fe43ca17…`) created, committed and anchored **before any run** — structurally identical to exec-1, diff confined to the pinned C*** hashes (cap `1f2d3366`, anchor `42075400`); emits only {verdict, reason, payload_sha256}; RATIFIED flipped in-process only. NOT RUN; corridor opens on separate authorization. `decision.RATIFIED = False`.
- **Version 35** — 2026-07-21. **D-30** (provenance closure): the execution-1 block runner committed verbatim as `run_block_execution1.py` (byte-equivalent `1d5a8623…`, D-18/19/20 precedent). Pins the C\* cap `28a73b3a` so it CANNOT run under C\*\*\* (would hard-fail the cap check) — a provenance record, not the corridor runner. No other generation path uncommitted. `decision.RATIFIED = False`.
- **Version 34** — 2026-07-21. **I-10.2** (pre-census supersession under C\*\*\*): C\*\* instance `8df3ff85…` (k over all controls) → C\*\*\* instance `1f2d3366…` (k over eval CLEAN, D-26); I-10 rule unchanged (I-10.1→I-10.2, versioned note). Dual tripwire cleared — k changed to the clean values (independently derived == cross-check == script), `route_cap = UNCONSTRAINED` identical to C\*\* (floor untouched). Derived-layer only, no replay. C\*\*/C\* records retained. `PRE_CENSUS.sha256` rollup `a8a47471…`. `decision.RATIFIED = False`.
- **Version 33** — 2026-07-21. **C\*\*\*** anchor (`CODE_ANCHOR` rollup `42075400…`, C\*→C\*\*→C\*\*\* lineage): closes **D-26** (`pre_census.py:51` k over eval CLEAN, one line) and **D-27** (relabel — `run_phase2a.py`/`gates.py` §4 whole-L diagnostics; field `budgets_matched_counts_eval_controls→s4_wholeL_diagnostic_counts`; grid decoupled from `C.BUDGETS`→`{0.01,0.02,0.05,0.10}`; artifact untouched, not re-run; `population.py` unchanged). Adds `tests_population_semantics.py`: **(B)** no clean gate outside the block (clearance guard, breaks build, negative-control verified) + **(A)** §6-denominator clean at block+pre_census, with the honest bound recorded. **D-27.C** (code/artifact field-name divergence disclosed). **D-25 amended** to three §6 divergences + one mislabelled §4 diagnostic (both guardrails). **D-24.2** (pre-census re-derivation expectation pre-committed: k changes, cap holds UNCONSTRAINED). `decision.RATIFIED = False`.
- **Version 32** — 2026-07-21. **D-29** (contamination clearance **RE-CERTIFIED** on three legs: category (b) provably empty; the whole-L PC1 TPR is over the wrong population to inform the clean analysis; no post-hoc tuning path — CLEAN registered pre-census). **D-10.R** (re-scoped enumeration approved: no purified-population indicator×label quantity ever existed on disk; the sole whole-L PC1 TPR+CI is `phase2a`, already public as D-2). **D-25.A** (disclosure amended to **seven findings / four divergences** + the attestation-defect category; neutral narration, no rigor-editorializing). D-25 paragraph updated accordingly. Next: the two denominator fixes (D-26/D-27) + population-semantics assertion → **C\*\*\***. `decision.RATIFIED = False`.
- **Version 31** — 2026-07-21. **D-28** (ATTESTATION DEFECT, seventh finding, PI-authored): the D-10 contamination enumeration is **incomplete** — its "no PC1 TPR/CI/PC2/power/denominator value has ever been surfaced" is false as written; `phase2a_accounting_and_gates.json` (mtime 2026-07-13, ~5 days pre-block) contains `PC1_TPR_at_b05`/`PC1_TPR_BCa95`/`TPR_minus_ref` (whole-L PC1 TPR+CI, `gates.pc1_gate`). **Defect in an attestation, not code/artifact**: the no-peek corridor held (execution-1 block gate quantities never written; 240-byte payload with `decision_quantities=null` remains true). The quantity is whole-L (§4), not Study-2 CLEAN; already public in D-2 (the Study-1 pathology headline); computed under gates.py's authorized-pre-ratification header. Ruling-3 certification must be **re-granted** against a corrected enumeration; D-10 to be re-scoped. Corridor shut pending re-cert. Exhaustive enumeration sweep ordered. `decision.RATIFIED = False`.
- **Version 30** — 2026-07-21. **D-27** (ANOMALY, sixth finding, PI-authored): a **second D-7-class denominator site** in the §4 accounting cluster — `run_phase2a.py:49` (matched-count k over ALL L controls) + `gates.py:52` (PC1/PC2 over `~is_event` = all L controls); `population.population_accounting` has no clean concept and feeds both. Same class + escape route as D-26 (neither calls `load_arm_units`); the corrected population-semantics axis surfaced it first pass. **Informational, not decisional**: `phase2a_accounting_and_gates.json` unanchored; `gates.py` imported only by `run_phase2a`; the operative gates/matched-counts/measurability are computed by the C\*\* producer over CLEAN and conform. Two informational sites carry the defect (D-26/D-27); the decisional path is clean-correct. Human-visibility enumeration check ordered. `decision.RATIFIED = False`.
- **Version 29** — 2026-07-21. **D-26** (ANOMALY, fifth finding, PI-authored): a **D-7-class divergence in the pre-census k table** — `pre_census.py:51` computes `k=round(b·ev["controls"])` over ALL eval controls, not eval CLEAN (wka72 §6); the clean counts sit unused at `:42`. Confirmed against every cell. Escaped because `pre_census` never calls `load_arm_units` and the D-7 fix + D-23 sweep were scoped to that function's consumers — **the correct audit axis is population semantics, not call-graph membership** (PI specification error). Bounded: the floor/route-cap uses `ev["clean_controls"]`, so `route_cap=UNCONSTRAINED` stands; the C\*\* pre-census `8df3ff85` inherited the nonconformant k table. Read-only checks ordered (k-table consumers; population-semantics re-sweep). No fix/re-derivation. `decision.RATIFIED = False`.
- **Version 28** — 2026-07-21. Pre-census re-derivation executed. **I-10.1** (supersession): old `28a73b3a…` → new `8df3ff85…` (registered grid), reason D-15, **I-10 rule unchanged**; `route_cap = UNCONSTRAINED` in both (D-24 tripwire clear — cap did not change). Derived-layer only (`rederive_pre_census.py`); C\* record retained. **I-5a conformance note**: floor is grid-independent fixed counts (≥100/≥100), primary b=0.05 common to both grids — no re-ratification. **D-25** (PI-authored disclosure, audit form): 30/34 conformant + 2 root-shared divergences; 3 committed writers; covariate cache proven row-subset; 4 findings each strictly upstream, terminating at the arXiv 2606.00329 GroupLens hash; provenance content-pinned end to end; the registered analysis has never been executed — Execution 2 is its first. `decision.RATIFIED = False`.
- **Version 27** — 2026-07-21. **D-24** (PRE-CENSUS RE-DERIVATION under C\*\*, cap expectation pre-committed): the anchored pre-census (`PRE_CENSUS`, route_cap=UNCONSTRAINED) carries the Study-1 k grid (spurious 0.01, no 0.20). Only the **derived layer** is recomputed (k table over the registered grid + floor/route-cap); the seeded-deterministic label-side rebuilds are **NOT re-run**. Cap **pre-committed = UNCONSTRAINED** (floor is a fixed count ≥100 clean/≥100 events over untouched populations; deterministic on unchanged inputs) — a changed cap is a **tripwire** (grid-entangled floor logic, R1-class): HALT + D-entry. Label-side counts + budget arithmetic only. `decision.RATIFIED = False`.
- **Version 26** — 2026-07-21. **S2-EXEC-2** (C\*\* anchor): fresh code anchor `CODE_ANCHOR` rollup `8322f560…` (commit `94e27dd`) closing **D-7** (population→CLEAN, real arm 21,725), **D-15** (budget grid `{0.02,0.05,0.10,0.20}`), **R1** (comment→wka72), and the **G** mtime=0 convention; plus E/F load-time gates (`conformance.py`). Covariate cache regenerated (D-23.A option 1): C\*\* `c338f55b` (21,725) proven a byte-exact row-subset of the C\* record `b7a0aa36` (3 clauses + precondition, `proof_covariate_subset.json` `736b5de2…`). `producer_test_results.json` regenerated (relationship-proven); verdict-suite unchanged. Conformance 12/12 + Q5. All anchors two-tier post-commit re-verified; `decision.RATIFIED = False`. Pre-census re-derivation + CP-2 + corridor deferred.
- **Version 25** — 2026-07-21. **D-23.A** (AMENDMENT): causal reframe — the D-7 fix did **not** break the cache; `b7a0aa36` carried a **latent inconsistency** (25,020-row cache whose corrected generating logic yields 21,725), its reproducibility conditional on the bug; regenerating under the corrected writer makes the anchored record consistent with the registration for the first time. Options rejected: (3) point-in-time = the exact D-18/19/20 defect class; (2) lenient-admission decouple = a second population-admission function (the one-admission-function principle governs). Invariance ground: the subset proof is sound only because pop_affinity is LOUO over all processed users (frozen I-2), so the proof **asserts** that precondition; the pattern must not be reused for population-dependent covariates. **Ruling: option 1** (regenerate + 3-clause subset proof). `decision.RATIFIED = False`.
- **Version 24** — 2026-07-21. **D-23** (SCOPE CORRECTION, PI-authored): the D-7 population fix is **not localized to producer.py** — `load_arm_units` is shared, so it changes `build_real_covariates.py`'s output (writer now yields 21,725 rows vs anchored 25,020, breaking byte-identity to `b7a0aa36` and the D-20 reproducibility claim). Defect in the C\*\* tasking spec, not the implementation. Analysis unaffected (block reads 25,020 cache, looks up 21,725 subset; all lookups succeed; values correct by LOUO row-set invariance). A–D edits validated and left uncommitted; read-only caller sweep ordered; ruling on the anchored cache in flight. (No D-22 was logged — the earlier "possible D-22" sorter check came back negative.) `decision.RATIFIED = False`.
- **Version 23** — 2026-07-20. **D-20.R** (RESOLUTION): covariate cache **re-derived byte-identically** on content-pinned input — the committed after-the-fact writer `build_real_covariates.py` reproduces the anchored cache decompressed `b7a0aa36…` (container `1de4001f` non-reproducible, not the test); input-gated on panel `ea972e2e` + ratings `360fb201` + contract; hash-only (no covariate value surfaced). Row-set invariance assertion committed + PASS (LOUO over full population n=162,206, not the cached n=25,005). INPUT_MANIFEST covariate entry de-provisionalised. **All three uncommitted writers now remedied** (D-18/D-19/D-20 committed; anchored artifacts unchanged). `decision.RATIFIED = False`.
- **Version 22** — 2026-07-20. **D-21.R** (RESOLUTION): raw-ratings integrity **ESTABLISHED against a public anchor** — `~/ml-25m.zip` sha256 `8b21cfb7…` == the anchor published in arXiv 2606.00329; its member `ratings.csv` == on-disk `9c855257…` (sha256+md5, streamed); the sorted file `360fb201…` is the committed-deterministic sort (`raw_input_provenance.py`, TRACKED) of it; → panel `ea972e2e…` → census `f286952c…` → verdict. Stale container pin `a6221995` retired (superseded, not repaired). No D-22 (sorter committed); uncommitted-writer set stays exactly three (D-18/D-19/D-20). GroupLens md5 flagged as unverified-from-memory for publication. Then: INPUT_MANIFEST for the 8 container-only artifacts; D-20 covariate re-derivation unblocked on content-pinned input. `decision.RATIFIED = False`.
- **Version 21** — 2026-07-20. **D-21** (ANOMALY, surfaced at the D-20 input gate): the raw-ratings provenance anchor is **stale and the input is currently unverifiable** — `raw_input_provenance.json` pins container sha256 `a6221995…`, but the live file's container is `6b0d833c…` and its decompressed content `360fb201…` (a6221995 matches neither; identical 160,627,922-byte size, consistent-but-not-proof of the D-16 gzip-mtime mechanism). No content hash exists anywhere for this file. **Blast radius confined to NEW derivation** (the D-20 covariate re-derivation, halted at its gate) — the slate panel (registered decompressed pin `ea972e2e…`) and the byte-identical census re-derivation are unaffected. Systemic: the container-only set is eight gzip artifacts (6 arm panels + covariate cache + this file); six still match, this one has drifted — the container convention rots silently. Read-only forensics ordered. No change/computation. `decision.RATIFIED = False`.
- **Version 20** — 2026-07-20. **D-20** (ANOMALY, D-19-class, found by the D-19 sweep): the anchored `real_covariates.csv.gz` has **no committed writer** — `covariates.py` only reads it; the precompute wrapper is unrecoverable (history + scratchpad empty). **Lesser than D-19**: the covariate computation logic (I-1/I-2) IS anchored in C\*; only the thin invoke-and-write wrapper is missing, so the artifact is reconstructable from anchored logic. The sweep did NOT return clean, so D-18's "last uncommitted path" claim is **not reinstated** (remains withdrawn per D-19). Full sweep inventory recorded; all other anchored artifacts have committed writers in C\*. Logged before remedy; covariate-writer remedy scoped by PI. `decision.RATIFIED = False`.
- **Version 19** — 2026-07-20. **D-19** (ANOMALY, D-18-class): the clean-control census **generator was never committed** (commit `6b6ac55` added only `.sha256` anchors; the original script is absent from git history and scratchpad, unrecoverable). Census content is byte-reproducible from committed state (R4, `f286952c…`, 109,955 bytes); remedy = commit the R4 re-derivation as the canonical generator (reproduces, did not produce). **D-18's "last uncommitted generation path" completeness claim is withdrawn** pending a systematic sweep of every anchored/registration-referenced artifact's generating code. Any further uncommitted-generator findings log as D-20+ before remedy. Plus RULING-9 deliverables (parameter diff table + machine-readable wka72 extract + config-equality test). No anchored analysis parameter changed; `decision.RATIFIED = False`.
- **Version 18** — 2026-07-20. **D-17** (D-16 RESOLUTION): content equivalence **established** — the live panel's DECOMPRESSED bytes hash to `ea972e2ef788…`, a full match to the registration's §4 pin; registration recorded a **content** hash, the divergent `71e5e6ca…` is only the gzip-container hash (pandas `to_csv` mtime), never registered. Registered artifact never overwritten (slate variant on a separate path). Canonical content hash `5dce843d…` recorded. **D-18** (ANOMALY): the slate-panel generating code (`compute_telemetry.py`, +5 lines atop `f03f7ad`) is uncommitted — artifact content-verified but its generation path absent from the anchored tree. Remedy (committed panel manifest + after-the-fact code commit + constancy corroboration) and closing verification (census byte-identity re-derivation) executed under this version. `decision.RATIFIED = False`.
- **Version 17** — 2026-07-20. **D-16** (ANOMALY, third finding, upstream of D-7/D-15): the live slate panel read by `config.py:15` hashes to `71e5e6ca…`, but registration §4 pins `…__slate_v1.csv.gz` at `ea972e2e…`; neither hash is in any committed manifest and the relog summary records no panel sha256, so content equivalence is unestablished from repo state. The panel is **input data** — indicators, degradation/starvation labels, and the anchored clean-control census (`6b6ac55`) are all downstream — so D-16 is adjudicated **before** D-7/D-15 remedies are scoped. Benign re-log/gzip-variation hypothesis explicitly **not assumed**. Read-only investigation ordered. No change made; `decision.RATIFIED = False`.
- **Version 16** — 2026-07-20. **D-15** (ANOMALY, second independent conformance finding): the anchored code carries the **Study-1** matched-budget grid `{0.01,0.02,0.05,0.10}` (`config.py:26`, `decision.py:33`, `criterion_2` at `decision.py:115`), but frozen wka72 §8 registers `{0.02,0.05,0.10,0.20}` (0.01 removed by design, 0.20 added; primary 0.05). Contaminates C\* (both files) and the anchored Phase-2.5 pre-census / route-cap (`PRE_CENSUS.sha256`). Orthogonal to the population-filter defect (D-7/D-9); the vacated verdict `fe6af9ed…` (PC1 gate-stop at primary 0.05) is unaffected. Routed §4.3. **No fix/recompute/re-anchor made under this entry**; correction scope + conformance-suite amendment pending PI ruling. `decision.RATIFIED = False`.
- **Version 15** — 2026-07-20. Study-2 **execution-1 verdict VACATED** for population nonconformance and reclassified as an executed-population nonconformance record (D-9); anchor `fe6af9ed…` stands unmodified, conformance void. Adds D-10 (execution-1 human-visible artifact enumeration — only the 4 null-ish payload fields + hash ever seen; no gate number ever surfaced), D-11 (maximal-disclosure pre-commitment: "second execution of the blind block" stated plainly), D-12/D-13 (asymmetry guards: a PC1 pass validates nothing about v1; there is no execution 3), D-14 (fix validates on label-permuted synthetic). Phase 4 returns to FULL hold; Step A permanently cancelled. Setup for a corrected first execution follows (Stages 1–3); the block is NOT authorized to run. `decision.RATIFIED = False`.
- **Version 14** — 2026-07-20. Phase-4 Step A halted. **D-7** (ANOMALY): the anchored verdict ran PC1 on the FULL L population (n=2,453 eval controls, k=123), not the purified population (n=766 eval clean controls, k=38); conformance to the registered Study-2 population is under adjudication against osf.io/wka72 — **no fix/recomputation/population change made; the anchor `fe6af9ed…` is not in question, conformance is.** **D-8**: the gate quantities were never written (no-peek held), so Step A is recomputation under §5, not extraction. `decision.RATIFIED = False`; no new number computed.

Anchors `DEVIATIONS.sha256` (Version 14), `CODE_ANCHOR.sha256` (C\* `096355fe…`), `STUDY2_CENSUS.sha256`, `SIGN_TABLE.sha256`, `VERDICT_SUITE.sha256`, `PRE_CENSUS.sha256`, `ARM_PANELS.sha256`, `COVARIATES.sha256`, and `S2_VERDICT.sha256` form the tamper-evident chain from freeze through verdict; the verdict is a §7 construction gate hard-stop and no §11 decision quantity was written or viewed. D-7 flags executed-population conformance (not the anchor) as under review.

Per §13 of the frozen pre-registration (OSF osf.io/7bvgz, registered 2026-07-09): *"any deviation from this document is disclosed in the paper with rationale and date."*

**Every entry below was made BEFORE any decision quantity (ΔTPR, AUC, the four criteria) was computed.** Phase 1 of the controls implementation was deliberately run as read-only spec-conformance verification, with no analysis execution, precisely so that these rulings cannot be post-hoc rationalizations of an unwelcome result.

**Unchanged and unchangeable (§13):** Δ = 0.05; incremental AUC ≥ 0.02; the dispositive status of the shuffled-timestamp control; the both-branches publication commitment. Nothing below touches any of these, and no §11 decision criterion is altered.

---

## D-1 — §4 descriptive claim is false as written (2026-07-09)
**Frozen text:** "Within L, `consecutive_misses` at step 20 ≤ 7 by construction: miss-based baselines retain signal without tautology."

**Fact:** False. The degradation rule fires only when `consecutive_misses ≥ 8` **AND** `remaining_frontier ≥ collapse floor (10)` (`build_user_episode_manifest.py:654–657`). Frontier-starved units (remaining frontier < 10) accumulate long miss streaks without being labeled degraded, and therefore sit in L legitimately. Observed in L (n = 25,020): **max `consecutive_misses` at step 20 = 19**; ~13% of L units carry ≥ 8 in-window consecutive misses, with `miss_run_fraction` saturated at 1.0.

**Ruling (David, 2026-07-09): descriptive-wording correction, disclosed. L is NOT re-scoped; no decision criterion is altered.**

**Rationale:**
1. The sentence is a *justification*, not an operative rule. L's operative definition ("no degradation declared at any step ≤ 20") is unchanged and correct.
2. The redefined indicators G, δ, p are pure functions of `slate_json` and are **unaffected**.
3. The error direction is **conservative**: it makes the miss-based baselines *stronger* for the affected units, raising the bar for ΔTPR ≥ 0.05. The mistake works against the hypothesis, not for it. Correcting it is therefore not a softening and does not implicate the §13 guardrail.
4. No §11 decision quantity depends on the claim.

**Paper action:** report the corrected statement with the counts above; note that the miss-based baselines are not tautology-free for the frontier-starved subgroup, and that this makes the comparison *more* demanding for S, not less.

---

## I-1 — §9 `log_activity` operationalization (2026-07-09)
**Ambiguity:** "log(1 + # pre-episode profile ratings)" — the panel/manifest offers `warm_start_positive_count` (size of the engine's positive profile) vs `n_total_ratings` / `n_positive_ratings`.

**Ruling: `warm_start_positive_count`.**

**Rationale:** (a) it is the literal referent of "profile" — the item set the engine actually conditions on — and it is non-degenerate (unlike `warm_start_ratings = 30`, correctly dropped); (b) `n_total_ratings` is **entangled with the label mechanism** (total ≈ warm-start + frontier + future, and frontier size feeds the degradation rule's `remaining_frontier ≥ 10` conjunct), so conditioning on it would import a label-adjacent quantity into the covariate set. No alternatives added (§9: "no additions post hoc").

---

## I-2 — §9 `pop_affinity` population scope (2026-07-09)
**Ambiguity:** over which user set is "# other users whose pre-episode profiles contain the item" counted — 162,541 processed / 40,339 included / L / per-fold?

**Ruling: all 162,541 processed users, leave-one-user-out.**

**Rationale:** counting within L or the included set would **condition on a label-related selection** — L excludes early degraders, and the `min_warning_runway` filter excludes *events only* — allowing label information to leak into a covariate. Item popularity is a property of the dataset, not of the analysis subsample. The broadest, label-independent set is therefore the only safe choice.

---

## I-3 — §7/§8 baseline scalarization for the threshold sweep (2026-07-09)
**Gap:** four of the six v1 families expose boolean alarm functions; a matched-alarm-count sweep requires a per-unit scalar score.

**Ruling: score = the family's internal statistic maximised over the ≤20 window; alarm iff score ≥ threshold.**

**Rationale:** semantically identical to the existing boolean alarm functions (an alarm fires iff the statistic exceeded threshold at some step in the window), and it is what makes a full sweep possible at all. `matrix_profile`, `permutation_entropy`, and `lag1_ac1` already return scalars. Effect is **conservative** — it gives the baselines their best operating point, raising the bar for S.

---

## I-4 — Pre-specified robustness split: the frontier-starved subgroup (2026-07-09)
**Why (specialist, ratifying D-1):** D-1's "conservative" claim is only *half* true. For criterion 1 (ΔTPR vs. best baseline) the error genuinely works against H1 — saturated miss streaks make the miss-based baselines stronger. **But the same ~13% may be systematically *easier* for S**, if frontier starvation correlates with contracted slates (high p, low δ) for reasons adjacent to the label mechanics rather than to genuine early warning. That is not §11-relevant — no criterion changes — but it is precisely the subgroup a hostile reviewer will probe.

**Pre-commitment (made before any decision quantity exists):** report the primary ΔTPR analysis **with and without the frontier-starved subgroup**, as a **pre-specified, non-decisional robustness split**. The §11 verdict is unchanged and is computed on full L regardless of what the split shows.

**Subgroup definition (one, logged): `remaining_frontier < 10 at step 20`.** Chosen over "in-window `consecutive_misses` ≥ 8" because it is the *mechanistic* definition — frontier depletion is both the conjunct that prevents labeling and the hypothesised driver of slate contraction — and because the frontier is monotonically non-increasing, so the condition is stable. Note the containment: within L, any unit with in-window `consecutive_misses ≥ 8` is necessarily frontier-starved (else it would have been labeled degraded), so the chosen definition is the broader, more conservative probe.

**If S's advantage lives entirely in this subgroup, we will have asked the question before the answer existed.**

---

## V-1 — Verification (NOT a deviation): baseline sign conventions before the sweep (2026-07-09)
I-3's max-over-window scalarization is equivalent to the boolean alarm functions **only if each family's statistic is monotone in alarm propensity.** CUSUM and Page–Hinkley are (threshold-crossing statistics by design). But if any family alarms on a *low* value — or if permutation entropy's degradation-relevant direction is *decreasing* — then max-over-window **silently inverts it**, and I-3's "conservative / best-operating-point" claim becomes false in the embarrassing direction.

**Required before the sweep:** a one-line check per family asserting the documented alarm direction against the frozen v1 calibration outputs. Logged as **verification, not deviation**. Ten minutes now versus a footnoted correction later.

---

## D-2 — 🔴 PC1 LEARNABILITY GATE FAILS → verdict routed to UNINTERPRETABLE-BY-CONSTRUCTION (2026-07-09)
**Recorded BEFORE any §11 decision quantity was computed (`decision.RATIFIED = False`; ΔTPR, incremental AUC, and the four criteria have never been evaluated).**

**Result.** At the primary budget b = 0.05, **PC1 (early miss rate) TPR = 0.0**, BCa CI [0.0, 0.0]. Fails under *both* readings of the §7 wording (TPR − chance = −0.05; TPR − prevalence = −0.805; both CIs exclude 0 on the wrong side), so the §7 wording ambiguity is moot for the verdict. Verified not a tie-handling bug (dedicated tie-heavy test passes).

**Mechanism — a label-validity failure, not a weak baseline.**
- L-events (degrade in (20,50]) miss **at most 7 of 20** in-window steps (PC1 median 0.05, max 0.35).
- L-controls miss a **median of 15 of 20** steps (PC1 median 0.75, max 0.95).
- Cause: the degradation rule fires only on `consecutive_misses ≥ 8` **AND** `remaining_frontier ≥ 10`. **Frontier-starved units can never be labeled degraded**, so they fail continuously and are filed as *controls* by construction. Since no L-event has ≥8 in-window misses, **all 3,267 miss-saturated units are controls — ~69% of L's entire control class (3,267 / 4,755).**
- Therefore in L, "control" does not mean "healthy user who did not degrade." It substantially means "user whose frontier ran out, who is failing constantly, and whom the rule cannot label." The event/control contrast is *will-degrade* vs. *(won't-degrade OR can't-be-labeled)*. Early miss rate is **anti-informative**: the highest-missing units are the controls.

**RULING (David, 2026-07-09): accept §7 as written. Option (a). Verdict = UNINTERPRETABLE-BY-CONSTRUCTION.** Per §7 verbatim: *"If PC1 fails, the landmark population carries no learnable signal and all indicator results are reported as uninterpretable-by-construction rather than as evidence."* Not SURVIVES, not DIES. The full sweep is still published per §12.

**Rationale — why amending would have been the worst available move.** Criterion 1 is ΔTPR = TPR(S) − TPR(**best baseline**), and every baseline is computed on the `miss_run_fraction` series now shown to be anti-informative in L. With the comparator family collapsed toward zero TPR, a **completely non-discriminative S would score TPR ≈ 0.05 at a 5% control-alarm budget by chance alone**, giving ΔTPR ≈ 0.05 — *exactly clearing criterion 1*. Amending the PC1 gate after observing the baseline collapse would therefore have manufactured a **false SURVIVES**: a published false positive, under a pre-registration, produced by loosening a gate in the direction that enabled the result. The gate did not rob us of a finding; it protected us from a fabricated one. §7's letter and the scientific substance align.

**Explicitly NOT done (would each constitute a new design, requiring a NEW pre-registration):** re-scoping L; flipping PC1's direction; excluding frontier-starved units; redefining the degradation label. A corrected-label study is the natural follow-up and will be separately pre-registered and disclosed as such.

**Consequences for execution.**
- §11 decision quantities are not evaluated as a survives/dies verdict.
- The **§10 control engines become moot** (criterion 4 exists to test recursion-specificity of a comparison that is uninterpretable). They are not built/run; this is disclosed, with the reason.
- The full sweep IS computed and published per §12 — labeled uninterpretable-by-construction — alongside the diagnosis above and the bounding diagnostic (§11 retirement clause).

**Why this is a contribution, not a dead end.** The frontier-floor conjunct in the degradation rule silently loads the control class with continuously-failing users, *inverting* miss-based early-warning signals. This is a structural pathology of the benchmark family; it **requires a re-reading of the v1 result** (direction not assumed — see the ratification addendum below), and anyone using this replay construction needs to know it.

### Ratification addendum (specialist, 2026-07-09)

**Sharpened contamination statement (use this in the paper, not the 69% figure).** Frontier starvation at step 20 **deterministically implies control status**. An event must hold frontier ≥ 10 at a collapse step > 20, and the frontier is non-increasing, so no L-event can be starved at 20: **starved-at-20 ⟹ control, with probability one** (asserted by direct query — L-events starved at 20 = 0). S sees slates; starved units plausibly have distinctive slates; therefore S could read the label's mechanical structure off the recommendation stream and clear criterion 3 with **zero early-warning content**. (Pure noise alone clears criteria 1–2 by arithmetic but FAILS criterion 3 — this deterministic channel is what makes the false positive live.) The gate did not block a finding; it blocked a measurement of the wrong quantity.

**Claim corrected (supersedes any "explains" wording).** Do **not** write that the pathology "explains" the v1 effect. Direction is not obvious — starved controls with contracted slates (high p, low δ) would push **control** means toward the v1 **event** pattern, **suppressing** the v1 triad effect, not inflating it. The pathology unambiguously indicts the v1 comparator layer and the label's construct validity. Correct phrasing: **"requires re-reading of the v1 result."** Upgrade only if the bounding diagnostic and descriptives support it.

**§13 defence for deferring the control engines.** §13 protects the shuffled-timestamp control's dispositive status **within a verdict computation**; when the gate routes to uninterpretable, no verdict is computed and there is nothing for it to be dispositive over. Its status is intact and waiting. **Deferred, not abandoned** — study 2 (the corrected-label follow-up) needs all five.

**V-1 results (verification discharged).** Per-family alarm-direction check against the frozen v1 alarm functions: **five families are monotone-increasing** in alarm propensity (`variance_ews`, `ac1`, `cusum`, `page_hinkley`, `matrix_profile`) — max/peak scalarization faithfully reproduces the boolean alarm. **Permutation entropy is decreasing** (v1 alarm `score ≤ threshold`, "low entropy = collapsed dynamics"); naïve max-over-window/≥-threshold **silently inverted** it. `v2_prereg_primary.py` already negated it correctly (lines 267–268); `v2_controls/baselines.py` did **not** and was **fixed** — negated, plus stable `mergesort` ordinal patterns to match the frozen v1 definition under ties. Cross-check: the two implementations now agree on PE to **0.0 over 1,600 comparisons** (400 units × 4 param settings). **Bias direction, for the record:** an inverted PE never wins "best baseline," so the best baseline was weaker and ΔTPR was **inflated in favour of S** — the bug biased toward the outcome we would have preferred. V-1 caught it before any number existed. This is a **conformance fix, not a deviation** (I-3 requires the scalarization be semantically identical to the boolean alarm; an inverted PE was not).

**Two counts (panel, step 20).** Total frontier-starved controls (`frontier_size_after < 10`) = **3,292 (69.2% of 4,755)** — the full number; the 3,267 miss-saturated figure (D-2 body) is a strict subset. L-events starved at step 20 = **0** (asserted).

---

## C-1 — Self-correction: the Version-1 log anchor was non-functional (2026-07-09)
The DEVIATIONS.sha256 committed at 73e65f9 contained the template placeholder `<PASTE_HASH_HERE>` rather than a computed SHA-256 — an operator error during the anchoring procedure. Consequence: 73e65f9 establishes a date but provides NO tamper-evidence for this log's contents. Disclosed rather than silently repaired; the commit is left unaltered in history. Version 2 carries the first real content hash, still committed before the sweep and before any decision quantity. The pre-decision timing of the D-2 ruling rests on three independent legs: (i) the contamination audit of disk and git history, which found that no ΔTPR, incremental AUC, or four-criteria evaluation was ever computed or written — the load-bearing leg; (ii) CODE_ANCHOR.sha256 at commit 08ca384, whose hashes are real and self-verifying; (iii) this Version-2 anchor. Recorded because an integrity artifact that hides its own failure is worthless.

---

## Paper-framing notes (from the conformance numbers — record now, use in the write-up)
- **L's event prevalence is 81% (20,265 / 25,020).** The landmark **softened, did not dissolve**, the v1 prevalence problem. Consequences: (a) the power gate is trivially cleared (10,153 vs 200); (b) at 81% prevalence, discrimination lives at *small* control-alarm budgets — the matched counts {25, 49, 123, 245} are well-chosen for exactly that regime, so the design holds; (c) **framing discipline:** never describe L as "the early-warning population." The honest sentence is *"the not-yet-degraded-at-landmark population, in a benchmark where degradation is the modal outcome."* If we don't write it, the hostile-review genre will write it for us.
- **43% of v1's events were already gone by step 20** (35,584 whole-horizon events → 20,265 landmark events; 15,319 early degraders excluded). Worth one descriptive sentence in the paper: it quantifies how much of the original v1 effect was measured on units for which *no warning was even conceptually possible*.

---

## S2-CENSUS — Study 2 clean-control viability gate: PASS (2026-07-17)
**Label-side accounting only** — read only `frontier_size_after` (frontier accounting); no indicator/decision quantity; `decision.RATIFIED = False`. Executed per the frozen Study 2 registration (OSF osf.io/wka72, §3) AFTER its hash anchor (commit `58721ac`; PDF SHA-256 `2374e370…` re-verified as a hard gate at run time).

**Definitions.** Candidate control = original control with `frontier_size_after ≥ 10` at step 20 (unstarved at the landmark). Starved in (20,50] = `frontier_size_after < 10` at any step 21–50. CLEAN = candidate that never starves through step 50.

**Counts.** Candidate controls = **1,463**; starved in (20,50] = **3** (first-starve steps 21/22/29; median 23; mean 24.3); **N(CLEAN) = 1,460** — calibration **694**, evaluation **766** — under the seedless SHA-256(ASCII userId) split (last hex ∈ {0–7} = calibration).

**Gate (frozen §3):** N(CLEAN) ≥ 1,000 → 1,460 ✓; per-fold N(CLEAN) ≥ 450 in **both** folds → cal 694 ✓, eval 766 ✓. **Verdict = PASS.**

**Route = VIABLE → PROCEED-TO-EXECUTION-PROTOCOL.** Per the registration, execution (random-arm code freeze before any real-engine computation) is a separate, later, specialist-reviewed session. No sweep is begun here; `decision.RATIFIED` stays False. The census artifact is kept local and gitignored (`results/v2_controls/study2_clean_control_census{.csv,_summary.json}`); its content hash is anchored publicly at `preregistration/STUDY2_CENSUS.sha256` (rollup `319a3b72…`).

---

## D-3 — Random control arm is degenerate (unmeasurable) under faithful frozen replay (2026-07-17)
**Finding (label-side/control-arm characterization; no real-engine quantity).** Built the §10 random control exactly as pre-registered — "sample top-K unseen items from a seeded RNG keyed on pre-episode ids" — and drove the **frozen** `simulate_user_episode`/`simulate_user_telemetry` unchanged. Result over the first 3,000 users (seed 101): **0 included**. Exclusions: `collapse_before_min_warning_runway` 1,906 (63.5%), `insufficient_total_ratings_for_warm_start` 572, `initial_frontier_below_remaining_frontier_floor` 522.

**Mechanism.** A uniform-random recommender picks 10 of ~40,858 items per step; a user's frontier is ~10–30 items, so P(hit) ≈ 0.5%/step. Essentially every eligible user accrues 8 consecutive misses by ~step 8 → collapses at ~step 8 → `warning_runway = 7 < 15` → excluded by `min_warning_runway`. So the random arm's landmark population **L₂ ≈ ∅ → UNMEASURABLE** (≪ I-5a's ≥100 clean-controls / ≥100 events floor).

**Disposition.** Logged, not repaired — uniform-random-over-unseen is the frozen spec; changing the construction would be post-hoc. The random arm is **non-dispositive** (only shuffled-TS and popularity gate the verdict), so this does **not** trigger UNINTERPRETABLE-BY-CONTROL-ARM. It is recorded as **additional pathology evidence**: the same frontier-floor/`min_warning_runway` interaction that loads the control class with continuously-failing users (D-2) also makes a chance recommender collapse instantly. **Debug vehicle** for the sweep/covariate/BCa machinery is therefore **label-permuted synthetic populations** (Phase 1.2's pre-specified fallback), never real-engine data. The dispositive arms (shuffled-TS runs the canonical CF engine on reordered data; popularity-only recovers popular hits) are expected measurable and are unaffected.

---

## Study-2 execution interpretations — ratified, logged BEFORE the pipeline code freeze (2026-07-17)
These six entries fix the control-arm decision logic in advance of building/running any arm. They govern the Study-2 execution pipeline (Phases 1–3). No real-engine indicator×label quantity is computed while logging them; `decision.RATIFIED = False`.

**I-5(rev) — control-arm gates are REPORTING-ONLY.** ΔTPR is computed **unconditionally** per control engine, on that engine's **own rebuilt population** (its own slates, labels, landmark L) with its **own per-arm best baseline**. §11 criterion 4 is satisfied for an engine **iff the criterion-1 bar is NOT met there** — i.e. ΔTPR point estimate < 0.05 **OR** the BCa 95% CI includes 0.

**I-5a — measurability + a new route.** An arm/seed is **measurable** iff its rebuilt evaluation fold has **≥ 100 clean controls, ≥ 100 events, and S non-degenerate** (≥ 20 distinct S values, IQR > 0). If a **dispositive** arm (shuffled-timestamp or popularity-only) is **unmeasurable**, the verdict = **UNINTERPRETABLE-BY-CONTROL-ARM** (claims neither SURVIVES nor DIES). This **explicitly extends §10's route list** to handle an unanticipated edge case (recorded as such, per the disclosure discipline).

**I-5b — absolute discrimination on control arms (non-decisional).** Report S's **absolute** discrimination on every control arm. If S's TPR at b = 0.05 exceeds chance (TPR − b, BCa 95% CI excluding 0) on a **recursion-severed** arm (random / shuffled-TS), the paper **must name mechanical channels** as a plausible contributor. Reporting-only; does not enter the verdict.

**I-6 — best-baseline re-maxed inside the bootstrap.** {six families ∪ PC1} is **re-maximised within each bootstrap replicate** (not fixed from the point estimate) when forming the paired ΔTPR distribution.

**I-7 — PC1 gate reference resolved (supersedes the D-2 wording flag).** PC1 "chance" = the budget **b**. Gate = **TPR_PC1 − 0.05 > 0**, BCa 95% CI excluding 0, direction increasing. (At the frozen b = 0.05 this is the only internally-consistent reading; the D-2 result — PC1 TPR = 0 — fails it regardless.)

**I-8 — shuffled-TS criterion-4 aggregation over seeds.** The shuffled-timestamp component of criterion 4 is satisfied **only if the criterion-1 bar is unmet on ALL FIVE seed replicates** (102 + {0..4}). **Any single seed meeting the bar blocks SURVIVES.** All five are reported (mean + range, per §9). Any **unmeasurable** seed → **UNINTERPRETABLE-BY-CONTROL-ARM** (as refined by I-9's dominance ordering); relaxation of this rule is possible **only via a visible D-entry**, never silently.

**I-9 — Verdict route ordering / C4 dominance lattice (ratified 2026-07-17).** *Principle:* unmeasurability negates only the claim that depends on the unmeasured quantity — SURVIVES depends on criterion 4, so a dead dispositive arm blocks SURVIVES; a DIES via criteria 1–3 depends only on the real arm (own gates passed, fully measurable), so a dead control arm cannot launder a measured null into "couldn't tell." **Every ambiguity resolves against the hypothesis.** Route lattice, top to bottom:
- **(i)** real-arm PC1/PC2 gate fail → UNINTERPRETABLE-BY-CONSTRUCTION; power gate → INDETERMINATE-BY-POWER
- **(ii)** any of criteria 1–3 fails → **SIGNAL DIES** (regardless of any arm's measurability)
- **(iii)** 1–3 hold **&** any MEASURABLE dispositive arm/seed meets the criterion-1 bar (retains) → **SIGNAL DIES** (C4 failed; dominates unmeasurability elsewhere)
- **(iv)** 1–3 hold **&** no measurable retention **&** any dispositive arm/seed unmeasurable → **UNINTERPRETABLE-BY-CONTROL-ARM**
- **(v)** 1–3 hold **&** all dispositive arms/seeds measurable **&** bar unmet on all five shuffled seeds and popularity → **SIGNAL SURVIVES**

Implemented in `decision.py`; pinned by the verdict test suite **12/12** (VERDICT_SUITE rollup `454df41b…`), mixed case (measurable popularity retains + one unmeasurable seed) = **DIES**. Refines I-8's "any unmeasurable seed → UNINTERPRETABLE" by ordering measurable retention (iii) ahead of unmeasurability (iv).

**I-10 — Phase 2.5 label-side control-arm pre-census (ratified 2026-07-17).** Before the blind block, run a **LABEL-SIDE** measurability census on the dispositive arms (shuffled-TS 102 + {0..4}, **each seed individually** per I-8; popularity-only): full §9 label-side rebuild → clean/event counts per eval fold → **I-5a LABEL-SIDE floor only** (≥ 100 clean controls, ≥ 100 events per eval fold). **No indicators / S / baselines / ΔTPR read** (same epistemic class as the Study-2 census). The indicator-side **S-non-degeneracy** half of I-5a stays **IN-BLOCK** (state this split explicitly in the artifact header). Also pin the per-arm realized **k(b)** table. Anchor census-grade. **ROUTING (cap-and-run, not terminate):** a pre-census dispositive-arm/seed failure does **not** end the study; it **CAPS** the route set at **{DIES, UNINTERPRETABLE-BY-CONTROL-ARM}**, the cap is declared and hash-anchored **BEFORE** the block runs, and the block then runs to determine which. All arms clear the floor → the block runs unconstrained.

**I-11 — No-relitigation guard (ratified 2026-07-17).** The pre-census reveals each control arm's rebuilt population shape (label-side foreknowledge). **Pre-commitment:** population-shape differences across arms are **expected consequences of the frozen §9 rebuild rule** and are **NOT** grounds for post-hoc reweighting, exclusion, or re-litigation of criterion 4; any such proposal is a **visible D-entry**.

**I-12 — FAMILY_PARAM_FLAG resolved: match the v1 families' parameter handling (full structural grid) (ratified 2026-07-18).** §7/§8 name "the six v1 families … each fully threshold-swept." **Fact from the frozen v1 code** (`calibrate_fast_families.fast_family_grids`, `calibrate_slow_families.slow_family_grids`): each family is a full `itertools.product(structural × threshold)` grid — variance_ews & ac1 over `window ∈ {5,9,13}`; cusum over `k ∈ {0.02,0.05,0.10}`; page_hinkley over `delta ∈ {0.005,0.01,0.02}`; matrix_profile over `window ∈ {4,5,6,7,8}`; permutation_entropy over `order ∈ {3,4} × delay ∈ {1,2}`. v1 evaluated **all** configs and selected the nearest comparator **across the full grid** (frozen ids `matrix_profile__44b81bd6`, `variance_ews__1a27323c`/`__a92c1715`, `permutation_entropy__14168dce` are distinct per-config hashes). There is **no single "v1-calibrated structural parameter"** to fix on. **Ruling:** each family's structural param grid **IS swept** (matching v1); the alarm threshold is swept **continuously** per §8 (superseding v1's discrete threshold grid). At a matched control-alarm count, a family's TPR = **max over its v1 structural grid**; best baseline = **max over {six families (each maxed over its structural grid) ∪ PC1}**. **Direction: anti-SURVIVES** — the faithful-to-v1 reading and the conservative tie-breaker (strongest baseline raises the bar S must beat) **coincide**; there is no tension. Governs the Phase-3 producer (to be built, debugged on non-real data, and re-frozen into a new C\* before the block runs).

---

## S2-EXEC — Study-2 execution-tree freeze (Phase 1 + Phase 2 build; 2026-07-17)
The Study-2 execution code is frozen and hash-anchored **before** the Phase 2.5 pre-census and the Phase 3 block are run, and **before any real-engine indicator×label quantity** exists. `decision.RATIFIED = False`; Phase 3 not entered.

**Anchored hashes.**
- **C\*** — `CODE_ANCHOR.sha256` rollup **`096355fe…`** over the completed producer tree (36 entries: code + the six control-arm indicator-side slate panels + the **precomputed real covariate cache** + producer/verdict test results + sign-table output). §9 covariates (`log_activity`, `pop_affinity` LOUO) are precomputed as data prep and anchored at `COVARIATES.sha256` (rollup `17037515…`); `compute_summaries` READS the cache so nothing runs first-time-at-scale in the block. **Projected full-block runtime ≈ 1 h** (ΔTPR 0.9 h + covariate ~4 min + gates/loading; the score-precompute→index-resample optimization is already in the producer). Earlier rollups: `48c1c3af…` (pre-covariate-cache), `36e255cd…` (pre-producer). The Phase-3 producer (`producer.py`: I-12 baseline sweep, ΔTPR per budget with cluster BCa + I-6 re-max, covariate signs + incremental AUC, gates → `RealArm`/`ControlArm`) is built and **synthetically validated** (planted signal → ΔTPR 0.91 + correct covariate signs + incr AUC 0.47; permuted → chance); `block_orchestrator.compute_summaries` wired to it, RATIFIED-guarded. The six control-arm slate panels are anchored at `ARM_PANELS.sha256` (rollup `b30ac8d0…`). Earlier this tree was frozen at `36e255cd…` (24 files, pre-producer): `controls/` (random, popularity, shuffled-ts replay engines), `indicators`, `baselines`, `sweep`, `bootstrap`, `covariate_model`, `decision` (C4-dominance lattice, I-9), `pre_census` (Phase 2.5, build-only), `block_orchestrator` (Phase 3, RATIFIED-guarded **+ cap-artifact-hash precondition**: the block verifies the anchored pre-census cap artifact's SHA-256 and refuses to run if absent/mismatched — I-10 enforced, not operational), `config`, `requirements-lock.txt`, `sign_table` generator + `v1_sign_convention_table.json` output, and `v2_prereg_primary.py`. (Version 6 first froze this tree at rollup `423f6591…`; Version 7 hardened `block_orchestrator` → `ce76b3de…`, no verdict changed.)
- **Sign table** — `SIGN_TABLE.sha256` rollup **`5b07aec9…`** (six families' alarm directions; PE decreasing & fixed; PC1 increasing).
- **Verdict suite** — `VERDICT_SUITE.sha256` rollup **`454df41b…`**; **12/12** known-answer cases pass incl. the C4-dominance mixed case (test 12 → DIES).
- **Synthetic-debug run-log** — `verdict_test_suite_results.json` sha256 **`a8d3cb62…`** — the label-permuted synthetic debug of the sweep/BCa/covariate machinery (chance by construction), recorded **in place of a random-arm run-log** because the random arm is degenerate (D-3).

**Governing ratified interpretations:** I-5(rev), I-5a, I-5b, I-6, I-7, I-8, I-9, I-10, I-11.

**Not run:** the Phase 2.5 pre-census (I-10, label-side) and the Phase 3 blind block are built but unexecuted, pending Cowork's CP-2 four-part precondition and pre-census code review. No real-engine ΔTPR / incremental AUC / four-criteria evaluation has been computed or written.

---

## S2-PRECENSUS — Phase 2.5 label-side pre-census: all arms clear the floor → route UNCONSTRAINED (2026-07-17)
Ran the Phase 2.5 label-side pre-census (I-10) under code freeze **C\* `36e255cd…`**, CP-2 authorized. **Label-side only** (read `frontier_size_after`/`step`/`label`/`collapse_step`; no indicator/S/baseline/ΔTPR). `decision.RATIFIED = False`; the block was not run.

**Artifact:** `results/v2_controls/phase25_pre_census.json`, SHA-256 **`28a73b3a…`**, anchored at `preregistration/PRE_CENSUS.sha256` (rollup `c8f110a5…`); payload gitignored/embargoed.

**I-5a label-side floor (≥100 clean controls AND ≥100 events per eval fold) — per arm/seed (eval fold):**

| arm/seed | eval clean controls | eval events | floor | k(b) = {0.01, 0.02, 0.05, 0.10} eval controls |
|---|---|---|---|---|
| popularity_only | 379 | 5,973 | ✅ | {13, 26, 65, 130} |
| shuffled_ts_102 | 1,086 | 14,617 | ✅ | {42, 84, 210, 420} |
| shuffled_ts_103 | 1,093 | 14,664 | ✅ | {42, 85, 212, 424} |
| shuffled_ts_104 | 1,087 | 14,553 | ✅ | {42, 85, 212, 424} |
| shuffled_ts_105 | 1,116 | 14,624 | ✅ | {43, 86, 214, 428} |
| shuffled_ts_106 | 1,124 | 14,617 | ✅ | {43, 86, 216, 431} |

**Route decision: all six dispositive arms/seeds clear the label-side floor → `route_cap = UNCONSTRAINED`.** The Phase 3 block, when authorized, runs **unconstrained** (no route cap). Note the S-non-degeneracy half of I-5a remains IN-BLOCK (indicator-side), unread here.

---

## S2-VERDICT — Study-2 §11 blind block: UNINTERPRETABLE-BY-CONSTRUCTION (2026-07-18)
The Phase-3 blind block was run **once** under real CP-2, as a single uninterrupted pass under code freeze **C\* `096355fe…`**: `real, popularity, shuffled_seeds = compute_summaries()` then `run_block(..., cap_artifact = phase25_pre_census.json, cap_artifact_sha256 = 28a73b3a…)` (route **UNCONSTRAINED**). `RATIFIED` was flipped **in-process** (anchored source stays False). Runtime 3,464 s.

**VERDICT: `UNINTERPRETABLE-BY-CONSTRUCTION`** — reason: **PC1 learnability gate fails (I-7)**. This is a gate hard-stop (route lattice step (i)): the §11 criteria (ΔTPR / covariate) were **not** the deciding path; the setup carries no miss-baseline-learnable signal at the primary budget, consistent with D-2. Per §7, all indicator results are reported as uninterpretable-by-construction, not as evidence; per §12 the full sweep is still published.

**No-peek attestation.** No human viewed any real-engine number before this commit. The block's information boundary emitted only `{verdict, reason, payload_sha256}`; on the gate hard-stop the decision quantities were **never written** (payload `decision_quantities = null`, 240-byte artifact). Verdict record `results/v2_controls/s2_verdict_payload.json` — file SHA-256 **`33e94369…`**, internal payload SHA-256 **`bfc7e0db…`** — anchored at `S2_VERDICT.sha256` (rollup `fe6af9ed…`); payload gitignored/embargoed.

**Route note.** Because criteria 1–4 were not reached, this is neither SURVIVES nor DIES; it is the §7 construction route. The v1-indicator bounding diagnostic (§11 retirement clause) and Phase-4 (remaining engines + non-decisional analyses) follow this commit.

---

## D-7 — ANOMALY: Study 2 executed-population conformance under review (2026-07-20)
Discovered post-verdict during Phase 4 Step A. `producer.load_arm_units` admits all controls to L2, flagging starved controls `clean=False` without excluding them; `producer.pc1_gate_ci` computes the matched count over all eval controls (n = 2,453, k = 123) rather than eval clean controls (n = 766, k = 38). The anchored verdict `fe6af9ed…` is therefore a true record of a gate run on the FULL L population. Whether this conforms to the registered Study 2 analysis population is under adjudication against the frozen osf.io/wka72 text. **No fix, no recomputation, and no population change has been made. The anchor is not in question; conformance is.**

## D-8 — Step A premise corrected (2026-07-20)
The gate quantities were never written (no-peek boundary held). Surfacing them is **recomputation under §5, not extraction**. Any Step A output will be labelled recomputed, with the code path and commit stated.

---

## D-9 — Verdict reclassification (2026-07-20)
`fe6af9ed…` (commit `a1f5220`, payload `33e94369…`, internal `bfc7e0db…`) is reclassified from "Study 2 verdict" to **executed-population nonconformance record**. The anchor stands unmodified as a true record of what was computed; its conformance to the registered analysis is **void**. Every downstream statement built on it is **withdrawn**, including "post-purification unlearnability" and the three-probe unification as previously stated. Adjudicated against verbatim frozen wka72: the executed population (2,453 eval controls, k=123) matches neither label B (1,460 CLEAN) nor label A (1,463 candidate); the 3,292 pre-landmark-starved units are excluded from L₂ by the population definition and were never admissible under any label. A corrected run is REQUIRED and is the **first execution of the registered analysis**, not a re-run of a displeasing result. **Do not alter, delete, or re-anchor `fe6af9ed…`.**

## D-10 — Execution-1 human-visible artifact enumeration (contamination-certification precondition) (2026-07-20)
Complete set of execution-1 quantities visible to any human: the four payload fields — `verdict = UNINTERPRETABLE-BY-CONSTRUCTION`, `reason = "PC1 learnability gate fails (I-7)"`, `route_cap = null`, `decision_quantities = null` — plus `payload_sha256 = bfc7e0db…`. The gate-record extraction was ordered but **never executed**; no PC1 TPR, CI, PC2, power, or denominator value has ever been surfaced to a human. No purified-population indicator×label quantity has ever existed. Enumeration complete; specialist certification of de minimis contamination attaches on this basis.

## D-11 — Disclosure pre-commitment, maximal and unhedged (2026-07-20)
DEVIATIONS and the paper will state: execution 1 ran on a **nonconforming population caused by a producer filter defect**; its full record publishes as D-7 with its anchors; the corrected run is the **first execution of the registered analysis**. The phrase **"second execution of the blind block"** appears in the paper with that clarification attached. No softening, no passive construction, no burial in an appendix.

## D-12 — Asymmetry guard 1 (2026-07-20)
A PC1 **pass** on the purified population retroactively validates **nothing** about v1. The bounding diagnostic and D-2 stand regardless of any execution-2 outcome. v1's recommender claims remain withdrawn on every branch.

## D-13 — Asymmetry guard 2 (2026-07-20)
**There is no execution 3.** The conformance assertions make silent nonconformance structurally impossible. Any further defect halts, publishes both records, and routes through §4.3.

## D-14 — Anomaly vehicle (2026-07-20)
The population-filter fix validates on **label-permuted synthetic data** (Phase 1.2 fallback), the random arm being degenerate.

---

## D-15 — ANOMALY: matched-alarm budget grid nonconformance (second, independent) (2026-07-20)
Discovered during Stage 1 preparation while deriving Stage 2 conformance expectations. The frozen registration (STUDY2-prereg-frozen.pdf, osf.io/wka72, hash 2374e370... verified live == anchor) specifies budgets b in {0.02, 0.05, 0.10, 0.20} of eval-fold CLEAN controls, primary b = 0.05, stating that b = 0.01 "is removed by design" and b = 0.20 "is added as a stable moderate-budget readout"; criterion 2 counts positive signs over that four-budget set. The anchored code carries the Study 1 grid (0.01, 0.02, 0.05, 0.10) at config.py:26 and decision.py:33, and decision.criterion_2 (decision.py:115) counts >= 3 of 4 over it. The anchored Phase 2.5 pre-census (phase25_pre_census.json, under PRE_CENSUS.sha256) and the DEVIATIONS floor table (line 232) were both derived on the Study 1 grid.

This defect is orthogonal to D-7 and predates it. It does not affect the vacated execution 1, which halted at the PC1 gate under primary b = 0.05, a member of both grids. It would bite any run reaching the sweep or criterion 2.

Recorded consequence: D-13 ("there is no execution 3") makes it impermissible to open the corridor with a known nonconformance in place. Stage 1 is halted; the "single localized change" instruction is falsified by fact and is superseded pending adjudication. No fix, no code change, no re-derivation and no new anchor has been made. A complete parameter-level audit of C** against the frozen registration is ordered before the remedy is scoped, so that the full defect set is known once rather than discovered serially.

---

## D-16 — ANOMALY: slate-panel provenance hash mismatch (third finding; upstream of D-7 and D-15) (2026-07-20)
Surfaced by the parameter-level conformance audit. Registration §4 pins the slate panel (...__telemetry_panel__slate_v1.csv.gz) at sha256 ea972e2e...; the live panel read by config.py:15 hashes to 71e5e6ca.... Neither hash is recorded in any committed manifest, and the slate-relog summary records the contract and engine hashes but not the panel's own sha256, so content equivalence cannot be established from repo state.

Severity note: the panel is input data, not a parameter. The indicators, the degradation and starvation labels, and therefore the anchored clean-control census (6b6ac55) are all downstream of it. Until content equivalence is established or refuted, every panel-derived quantity is unverified, including the population counts that the planned Stage 2 conformance assertions were to be derived from. D-16 is therefore adjudicated before D-7 and D-15 remedies are scoped.

A post-freeze re-log with byte-level gzip variation (timestamp-sensitive compression) is the leading benign hypothesis and is explicitly NOT assumed. No change made.

---

## D-17 — D-16 RESOLUTION: content equivalence established; benign mechanism confirmed by evidence (2026-07-20)
Registration §4 pins the slate panel at ea972e2e.... The live panel's DECOMPRESSED bytes hash to ea972e2ef788801b8e947868fbcf9ea9250393ba88f67f5061dacefced930541 -- a full match. The registration therefore recorded a CONTENT hash, not a container hash, and the live panel conforms exactly. The divergent 71e5e6ca... is the gzip container hash, which appears in no manifest and was never the registered quantity. Mechanism: pandas to_csv(compression="gzip") embeds a build mtime in the gzip header, producing non-reproducible container bytes over identical content. The registered artifact was never overwritten -- the slate variant was written to a separate path (results/v2_prereg_slate_relog/...__slate_v1.csv.gz); the base panel still exists. Corroborating facts: 1,093,072 rows; 24 columns; user_id 2..162,541; step 1..50, consistent with the frozen population and horizon. Canonical content hash (rows sorted by (user_id, step), header + comma-joined verbatim fields), recorded for future re-compression- and reorder-independent verification: 5dce843d4df2bbc7f2640d49ff0fa94d0bbc4fd717ff6b44393026028f065427. Root gap identified and remedied at D-17.R below: no committed manifest recorded any panel hash, because the panel is untracked -- present locally, never committed, not gitignored. D-16 was logged before this investigation began; that ordering is the credibility record.

## D-18 — ANOMALY: slate-panel generating code is uncommitted (2026-07-20)
compute_telemetry.py carries +5 uncommitted lines atop commit f03f7ad (2026-05-29) adding slate_json and slate_ablated_json; write_panel() line 679 produced the registered-content panel. The artifact is content-verified against the registration (D-17), but its generation path is absent from the anchored tree and therefore not reproducible from repo state. No claim is made that the artifact is wrong; the finding is that its provenance chain has an uncommitted link. Logged before remedy.

---

## D-19 — ANOMALY: clean-control census generator was never committed (2026-07-20)
Surfaced as a byproduct of the R4 closing verification. Commit 6b6ac55 added only the .sha256 anchors; the script that produced the census artifact is absent from git history and from the scratchpad and is not recoverable. The census content is nonetheless byte-reproducible from committed state: R4 re-derived it from the content-verified panel under anchored label-side logic (fold(), floor 10, window 20, starve-window (20,50]) to byte-identity, sha256 f286952c..., 109,955 bytes. Remedy adopted: rather than recover an unrecoverable original, the R4 re-derivation is committed as the canonical census generator, with disclosure that it reproduces -- and did not produce -- the anchored artifact. This is the same class as D-18 and materially affects D-18's completeness claim: the assertion that D-18 was the last uncommitted generation path was NOT verified by sweep and is hereby withdrawn pending the sweep ordered below.

---

## D-20 — ANOMALY: real-covariate cache has no committed writer (D-19-class; found by the D-19 sweep) (2026-07-20)
The systematic sweep ordered in D-19 (Task 1) returned one further N row. The anchored artifact `results/v2_controls/real_covariates.csv.gz` (anchored under `COVARIATES.sha256` and in C\*, added by commit `b4aea20` "precompute covariate cache") has **no committed code that writes it**: `covariates.py` only READS it (`load_real_covariates`, line 79). No writer exists in the committed tree, in git history (`git log -S` empty), or in the scratchpad. The precompute wrapper that produced the cache is unrecoverable.

**Severity — lesser than D-19.** Unlike the census (whose entire generation logic was uncommitted), the covariate **computation logic IS anchored in C\***: `covariates.py` holds `log_activity_from_manifest` (I-1), `build_pre_episode_profiles` + `pop_affinity` (I-2, LOUO over 162,541), all frozen. Only the thin orchestration-and-write wrapper (call those functions → write the two-column cache) is absent. The artifact is reconstructable from anchored logic; the missing link is the invocation/serialization wrapper, not the method.

**Effect on D-18's completeness claim.** The D-19 sweep did NOT return clean (this D-20 is a further finding), so D-18's withdrawn "last uncommitted generation path" assertion is **not reinstated** — it remains withdrawn per D-19. Enumerated inventory of uncommitted generation paths found by the sweep: (a) slate/base telemetry panels → D-18 (remedied: `compute_telemetry.py` committed); (b) clean-control census → D-19 (remedied: canonical generator committed, reproduces `6b6ac55` byte-identically); (c) real-covariate cache → D-20 (this entry; logic anchored, wrapper missing). All other anchored artifacts have committed writers in C\* (`pre_census.py`, `generate_arm_panels.py`, `sign_table.py`, `block_orchestrator.py`, `run_phase2a.py`, `tests_producer.py`, `tests_verdict_suite.py`).

**Disposition.** Logged before remedy per D-19. No fix, no code change, no re-anchor made under this entry; the covariate-writer remedy is scoped by the PI (the cache itself is untouched and its content anchor stands). `decision.RATIFIED = False`.

---

## D-21 — ANOMALY: raw-ratings provenance anchor is stale and the input is currently unverifiable (2026-07-20)
Surfaced at the D-20 input gate. raw_input_provenance.json records sha256 a6221995... for ratings__sorted_by_user_time.csv.gz. The live file's container hashes to 6b0d833c... and its decompressed content to 360fb201...; a6221995 matches neither. Container size is identical (160,627,922 bytes), consistent with -- but not proof of -- the D-16 gzip-mtime mechanism, since the mtime occupies four bytes of a fixed-width header. Unlike the slate panel, whose registration pinned decompressed content and passes, the raw-ratings provenance pinned only a container hash and no content hash exists anywhere, so the input cannot presently be content-verified against any anchor.

Blast radius, stated: artifacts already derived from this input AND independently content-verified downstream are unaffected -- the slate panel matches its registered decompressed pin ea972e2e..., and the clean-control census re-derives byte-identically from that panel. The exposure is confined to NEW derivation from the ratings file, i.e. the D-20 covariate re-derivation, which is halted at its input gate with no computation performed.

Systemic context: the anchor-convention sweep identifies a container-only set of eight gzip artifacts with no content hash recorded -- six arm slate panels, the covariate cache (1de4001f...), and this file. Six still match their container anchors; this one has already drifted, demonstrating that the container convention rots silently. D-21 is logged before investigation.

---

## D-21.R — RESOLUTION: raw-ratings integrity ESTABLISHED against a public anchor (2026-07-20)
The stale pin a6221995... is retired as a container hash of a derived intermediate, superseded rather than repaired. Integrity now rests on a published anchor plus committed deterministic derivations:

  published zip anchor 8b21cfb7... (arXiv 2606.00329)  ==  ~/ml-25m.zip, verified
    -> zip member ml-25m/ratings.csv                   ==  on-disk ratings.csv 9c855257... (sha256 and
                                                           md5 b0e86638... both verified, streamed,
                                                           nothing written)
    -> committed deterministic sorter raw_input_provenance.py (TRACKED, f03f7ad):
       sort_values[userId, timestamp, movieId] + to_csv LF
    -> sorted file decompressed 360fb201...  (independently PROVEN the deterministic sort of
       ratings.csv: identical 25,000,095-line multiset by commutative content hash 7be0c7af...,
       monotonic by the documented key, sort key byte-unique under (userId, movieId) uniqueness)
    -> slate panel decompressed ea972e2e...  == registered §4 pin (D-16/D-17)
    -> clean-control census f286952c...      == 6b6ac55, byte-identical (D-19/R4)
    -> verdict payload, decision_quantities = null

Mechanism evidence for the container drift: gzip header mtime 2026-07-09 09:49:06Z places the regeneration on the registration draft date, after the a6221995 anchor was committed in f03f7ad (May 2026); the slate panel was regenerated 18 minutes later at 10:07Z with content provably preserved. Composition figures (25,000,095 ratings; 162,541 users; 1995-2019 timestamp range) are recorded as corroborative color, not as the basis of the finding.
No D-22: the sort/serialization writer is committed and tracked. The uncommitted-writer set remains exactly three (D-18, D-19, D-20).
Note for publication: the GroupLens md5 6b51fb27... is asserted from general knowledge, not verified against GroupLens's published checksum, and must be source-verified before it appears in the paper. The load-bearing verification is the sha256 against the anchor published in arXiv 2606.00329.

---

## D-20.R — RESOLUTION: covariate cache re-derived byte-identically on content-pinned input (2026-07-20)
The D-20 covariate re-derivation, halted at D-21 pending a content-verified raw-ratings input, is unblocked and complete. The after-the-fact canonical writer `build_real_covariates.py` (committed, references D-20) reproduces the anchored cache `real_covariates.csv.gz` at the DECOMPRESSED-content level, byte-identical: `b7a0aa3615966c82…`. Container identity (`1de4001f…`) is non-reproducible (gzip mtime, D-16 mechanism) and was not the test. The writer REPRODUCES — did not produce — the anchored artifact; it calls the anchored `covariates.py` functions verbatim (I-1 `log_activity`, I-2 `pop_affinity` LOUO) so float arithmetic is bit-identical, with the users-without-profile default `pop_affinity=0.0`.

Input gate (all passed before any computation): slate panel decompressed `ea972e2e…` (registered §4 pin), sorted ratings decompressed `360fb201…` (INPUT_MANIFEST; integrity D-21.R), contract constants (`contract_freeze.json`, `load_frozen_constants` asserts). HASH-ONLY throughout: no covariate value was printed, sampled, sorted, or otherwise surfaced — comparison was on the decompressed content hash alone.

Row-set invariance assertion committed and PASS: `pop_affinity` is LOUO over the full processed population (n_full=162,206 profiled users) — its value for a shared test user differs from the same computation restricted to the cached subset (n_sub=25,005), so the cache column is invariant to which rows are written (values compared, never printed).

INPUT_MANIFEST covariate-cache entry updated: `decompressed_sha256 = b7a0aa36…` is now re-derivation-verified (provisional flag retired). Uncommitted-writer set from the D-19 sweep is now fully remedied: D-18 (panels → `compute_telemetry.py`), D-19 (census → canonical generator), D-20 (covariate cache → this writer) — all three committed; the anchored artifacts are unchanged. `decision.RATIFIED = False`.

---

## D-23 — SCOPE CORRECTION: the D-7 population fix is not localized to producer.py (2026-07-21)
The C** tasking specified the D-7 remedy as three producer.py sites (84, 98, 123/145). load_arm_units is shared, so the fix also changes the output of build_real_covariates.py (the D-20 canonical writer), whose cache row-set is derived from it: post-fix the writer yields 21,725 rows (20,265 events + 1,460 clean controls) rather than the anchored 25,020, breaking byte-identity against b7a0aa36... and therefore the D-20 reproducibility claim. This is a defect in the tasking specification, authored by the PI, not in the implementation. The analysis itself is unaffected: the block reads the 25,020 cache and looks up a 21,725 subset, every lookup succeeds, and values are correct because pop_affinity is LOUO over the full processed base and row-set invariant (proven, D-20.R). Logged before remedy.

---

## D-23.A — AMENDMENT: causal reframe and invariance ground (2026-07-21)
**Reframe (ratified).** The D-7 fix did not break the covariate cache's reproducibility. b7a0aa36 was anchored with a latent inconsistency already inside it: a 25,020-row cache whose own generating logic, corrected to specification, produces 21,725. The C* cache was throughout the artifact of the defective admission path, and its reproducibility was conditional on the bug. Regenerating under the corrected writer is therefore not a workaround — it is the anchored record becoming consistent with the registration for the first time.

**Options rejected, with reasons preserved.** (3) accept-as-point-in-time was rejected because asserted-not-re-runnable is the exact defect class D-18, D-19 and D-20 were opened to close. (2) decoupling the writer via a lenient admission path was rejected because it would install a second population-admission function in a codebase whose defining bug was population admission. The one-admission-function principle is treated as the governing lesson of this audit arc.

**Invariance ground.** The subset proof is sound only because pop_affinity is leave-one-user-out over all processed users per the frozen I-2 ruling, making every row's value independent of which rows the cache admits. Row-set invariance is a property of that definition, not a property of caches. Had the LOUO base been the admitted population, option 1 would silently change values and the subset assertion would correctly fail. The proof script therefore ASSERTS the invariance precondition rather than assuming it, and the subset-proof pattern must not be applied to any future covariate whose definition is population-dependent.

Attribution unchanged: the specification error was the PI's.

---

## S2-EXEC-2 — C** anchor: closure of D-7, D-15, R1, and the G convention (2026-07-21)
Fresh code anchor **C\*\*** = `CODE_ANCHOR.sha256` rollup `8322f5600841ec270215a868d2eaba8c15fd11bb576fc62c6621e657fb758724` (committed `94e27dd`), superseding C\* (`096355fe`, `b4aea20`). Anchored BEFORE the blind block and BEFORE any real-engine indicator×label quantity; **`decision.RATIFIED = False`** in anchored source throughout. Contamination certification: the de minimis basis enumerated in D-10 holds — this build computed no decision quantity; the only artifact regenerated (covariate cache) is label- and indicator-agnostic (I-2 / D-20 / D-23.A). One admission function (`load_arm_units`); the D-23 shared-consumer set is closed (covariate cache + `producer_test_results.json`; caller sweep at v24).

**Complete diff.**
- **A (D-7 population filter, producer.py):** `:84` in_L2 admits a control iff CLEAN (never starved); `:98` z-parameters on `(fold==cal) & clean_control` (cal-fold CLEAN, 694); `:123/:145` matched-count denominator is eval CLEAN (766) by purification. Real arm 25,020→**21,725** (20,265 events + 1,460 clean). Validated on label-permuted synthetic (D-14): planted ✓, permuted/chance ✓, clean-determination ✓.
- **B (D-15 budget grid):** `config.py:26` + `decision.py:33` `BUDGETS → (0.02,0.05,0.10,0.20)`; criterion_2 `≥3/4` logic unchanged.
- **C (R1):** `config.py:5-6` provenance comment → wka72 (commit `58721ac`, PDF `2374e370…`) + `wka72_registration_params.json` as source of truth.
- **D:** conformance test **12/12** (was 11/12) + **Q5** budget-closure assertion (grid drift now breaks the build); row-set invariance present + passing.
- **E (input gate, conformance.py):** content (decompressed) pins, never containers — panel `ea972e2e…`, ratings `360fb201…`, covariate cache **`c338f55b…`** (C\*\* hash); mismatch hard-stops before compute.
- **F (population conformance, conformance.py):** per-class per-fold set-hash vs census `6b6ac55` (derived, not hardcoded); census cross-checked vs registration (20,265 / 1,463 / 1,460 = 694+766, §4.3 on disagreement); k = {15,38,77,153} computed from the census; wired into `compute_summaries` before any computation; negative control confirmed hard-stop.
- **G (deterministic compression):** all gzip artifact write paths use `mtime=0` (`build_real_covariates`, `generate_arm_panels`, `compute_telemetry`, `raw_input_provenance`); ephemeral/temp writes exempt. Closes the container-drift class (D-16/D-21) going forward.

**Covariate cache regeneration (D-23.A option 1).** C\*\* execution cache `real_covariates.csv.gz` = decompressed **`c338f55b…`**, container `408affb3…` (mtime=0), 21,725 rows. Byte-exact row-subset of the C\* record `b7a0aa36…` (25,020, preserved at `real_covariates__c-star-25020.csv.gz`) — proof `proof_covariate_subset.json` (`736b5de2…`): precondition (pop_affinity LOUO population-independent, I-2) PASS; (i) all 21,725 rows byte-present PASS; (ii) 3,295 absent == D-7 exclusion set, set-hash vs census PASS; (iii) schema identity PASS. `producer_test_results.json` regenerated `052a959e→be965982` (relationship: exactly the 10 synthetic starvers excluded; n_clean and every other KAT assertion unchanged). `verdict_test_suite_results.json` **unchanged** (B inert for its synthetic data).

**Anchors (all two-tier, post-commit re-verified):** CODE_ANCHOR `8322f560`; INPUT_MANIFEST (C\*\* cache pinned, `b7a0aa36` reclassified as C\* record superseded-for-execution, proof cross-referenced); COVARIATES re-pointed to the preserved C\* record; REGISTRATION_PARAMS re-anchored (12/12 + Q5). S2-VERDICT / STUDY2_CENSUS / PRE_CENSUS untouched. **Deferred (separately authorized):** pre-census re-derivation under C\*\* (reads `config.BUDGETS`), the I-10.1 / cap-tripwire amendment, extended CP-2, then the corridor.

---

## D-24 — PRE-CENSUS RE-DERIVATION under C**, with cap expectation pre-committed (2026-07-21)
phase25_pre_census.json (PRE_CENSUS.sha256, route_cap = UNCONSTRAINED) was derived on the Study-1 budget grid: its per-arm k table carries a spurious b = 0.01 column and lacks b = 0.20. The artifact is therefore stale under the registered grid and cannot feed the I-10 precondition unamended.

Scope, ruled: the underlying label-side rebuilds (popularity + five shuffled-TS seeds: slates -> labels -> starvation -> landmark -> per-fold counts) are grid-independent and seeded-deterministic and are NOT re-run. Re-running them near a pending corridor buys nothing and invites a result-shopping reading. Only the derived layer is re-computed: the per-arm k table (k = round(b.n) over the registered grid) and the floor / route-cap evaluation, which is re-executed even though its inputs are unchanged.

Cap expectation, pre-committed before any number exists: the unmeasurability floor is a fixed count (>=100 clean / >=100 events), the populations are untouched, and the cap is therefore a deterministic function of unchanged inputs. It CANNOT legitimately change. Expected outcome: UNCONSTRAINED. A changed cap is NOT a result to accept — it is a tripwire proving the floor logic was grid-entangled in code, an R1-class defect. On a changed cap: HALT, log a D-entry, investigate, do not proceed.

Contamination: label-side counts and budget arithmetic only; no indicator is paired with any label. Safe pre-corridor, same class as the census itself.

---

## I-10.1 — Pre-census artifact supersession (2026-07-21)
Old hash `28a73b3aad983b201c275ae159d24e453ed4a069cde79cfd1b616b11d27e3ee9` (Study-1 k grid) → new hash `8df3ff85cf2065e443dd42d5783366ebe9cd08ba1871f9911efee8ba42777c8e` (registered grid {0.02,0.05,0.10,0.20}). Reason = **D-15**. **The ratified I-10 rule is UNCHANGED** — cap-and-run routing on the I-5a label-side floor; all arms clear → block runs UNCONSTRAINED. Ratification covers rules; artifacts are instances; the instance is superseded under the same rule with this versioned note, not re-argued. `route_cap = UNCONSTRAINED` in both instances (D-24 tripwire clear: the floor is a fixed count over untouched populations — it did not, and cannot, change). Re-derivation was derived-layer only (`rederive_pre_census.py`, from the anchored counts; label-side rebuilds not re-run); the C* record is retained at `phase25_pre_census__c-star.json` under `PRE_CENSUS.sha256`. `decision.RATIFIED = False`.

## I-5a conformance note — registered-grid audit (2026-07-21)
The I-5a label-side floor rationale was audited against the registered grid. The floor is the fixed count **≥100 CLEAN controls AND ≥100 events per eval fold** — grid-independent. The operative budget **b = 0.05 is common to both grids**; the ratified I-5a text refers to fixed counts, not to any budget-specific quantity. **No re-ratification is required.** (The per-arm k table is informational — "control counts only," over each arm's eval control count — and does not feed the route-cap; only the floor does.)

## D-25 — DISCLOSURE, audit form (supersedes the earlier single-defect language) (2026-07-21)
[Amended by D-25.A to seven findings / four divergences; original four-finding text above the fold retained in the log history at v28.] An exhaustive parameter-conformance audit of the executed configuration against the registration found 30 of 34 parameters conformant. Three §6 arithmetic divergences were found, plus one mislabelled §4 diagnostic. Two divergences surfaced on the parameter axis — the population filter and the matched-alarm budget grid — sharing one root cause: constants transcribed from the Study-1 registration rather than the operative Study-2 one. A re-sweep on the correct axis, population semantics rather than call-graph membership, found a third of the same class: the pre-census matched-count k table, computed over all controls rather than eval CLEAN controls. All three are remediated in code and anchored — the parameter pair under C**, the pre-census denominator under C***. The same re-sweep found a fourth site of a different kind: a §4 whole-L diagnostic dressed in §6 nomenclature — matched-count budgets, a §6-style field name, output that read as the registered operating points — and it has been relabelled. This is a fault, not an exculpation: the quantity was presented as something it is not. What it actually is: the whole-L PC1 TPR and BCa CI, already anchored and published as the D-2 pathology result, computed over all controls. Its computation is correct for its purpose; only its dress was wrong, and the remedy is code-only relabelling under C*** — the on-disk artifact, the values, the grid and the population are untouched. A generation-path sweep found three uncommitted writers; each was subsequently committed and its artifact content-verified or canonically re-derived byte-identically. The covariate cache was regenerated under the corrected admission path and proven a byte-exact row-subset of the prior anchor, with the exclusion set verified against the census by set-hash. Seven findings were logged, each before its investigation: the population filter, the parameter transcription, the panel container provenance, the raw-input provenance, the pre-census k denominator (a §6 arithmetic divergence), the §4-accounting diagnostic mislabelled as §6 (a labeling defect, relabelled not recomputed), and an attestation defect — the initial contamination enumeration was incomplete, having omitted a whole-L PC1 TPR and BCa CI in phase2a_accounting_and_gates.json that is already public as the Study 1 pathology headline; an exhaustive population-semantics sweep found it, the enumeration was corrected, and contamination clearance was re-certified against the verified-empty Study-2 clean category. The first four findings are strictly upstream of one another — population filter, parameter transcription, panel container provenance, raw-input provenance — terminating at a public anchor outside the project: the GroupLens distribution hash published in arXiv 2606.00329. The provenance chain is content-pinned end to end, from that published hash through ratings.csv, the sorted input, the slate panel's registered pin, the clean-control census, and the covariate cache, to the verdict function. The registered analysis has never been executed. Execution 2 is its first execution, under a configuration corrected and proven conformant by anchored audit, on inputs proven identical to the registered pins.

---

## D-26 — ANOMALY: D-7-class divergence in the pre-census k table (fifth finding) (2026-07-21)
pre_census.py:51 computes kb = {b: round(b * ev["controls"])} where ev["controls"] (pre_census.py:41) counts ALL eval-fold controls with no clean filter. wka72 §6 specifies budgets "of eval-fold CLEAN controls". The correct clean counts exist adjacently at pre_census.py:42 and are unused by the k line. Confirmed against every cell: reported k equals round(b x all-eval-controls) exactly for all six arms and all four budgets.

Registered-correct k, for the record:
  popularity_only  eval clean 379  -> {8, 19, 38, 76}      (reported {26, 65, 130, 260})
  shuffled_ts_102  1086            -> {22, 54, 109, 217}   (reported {84, 210, 420, 840})
  shuffled_ts_103  1093            -> {22, 55, 109, 219}
  shuffled_ts_104  1087            -> {22, 54, 109, 217}
  shuffled_ts_105  1116            -> {22, 56, 112, 223}
  shuffled_ts_106  1124            -> {22, 56, 112, 225}

Why it escaped: pre_census builds populations via replay.run_arm_labelside -> LabelSideUnit -> _fold_summary and never calls load_arm_units. The D-7 fix and the D-23 consequence sweep were both scoped to load_arm_units consumers. The sweep was correctly executed against an incorrectly specified scope; the specification error was the PI's. The correct audit axis is population semantics -- every site that partitions, counts or filters controls -- not call-graph membership of one function.

Bounded so far: the floor / route-cap uses ev["clean_controls"] and ev["events"], not ev["controls"], so route_cap = UNCONSTRAINED was computed on purified inputs and stands. The C** pre-census instance (8df3ff85) inherited the nonconformant k table. Full consumer set of the k table is under investigation. D-26 logged before investigation and before any remedy.

---

## D-27 — ANOMALY: second D-7-class denominator site, in the §4 accounting cluster (sixth finding) (2026-07-21)
Surfaced by the corrected-axis re-sweep. run_phase2a.py:49 computes a matched-count k table as round(b x accounting["eval_controls_in_L"]) over ALL L controls, and gates.py:52 computes the PC1 and PC2 gates over (~is_event).sum(), also all L controls. population.population_accounting (population.py:90, 103) counts label == "control" with no clean concept -- correct as §4 whole-L composition, but it feeds both divergent sites. wka72 §6 requires eval-fold CLEAN controls.

Same defect class and same escape route as D-26: neither module calls load_arm_units, so neither was in the D-23 sweep scope. The corrected audit axis -- population semantics rather than call-graph membership -- surfaced it on the first pass.

Severity, as established: informational, not decisional. phase2a_accounting_and_gates.json is not anchored; gates.py is imported only by run_phase2a and never by block_orchestrator or producer. The operative PC1, PC2, power, matched-count and measurability quantities are computed by the C** producer and block over the CLEAN population and CONFORM. The pre-census floor and route cap use clean counts and CONFORM. No divergent site reaches any decision quantity.

Standing state: two informational sites carry the all-vs-clean denominator defect (D-26 pre_census.py:51; D-27 run_phase2a.py:49 + gates.py:52). The entire decisional path is clean-correct. Remedy scope is under adjudication. Logged before remedy.

---

## D-28 — ATTESTATION DEFECT: the D-10 contamination enumeration is incomplete (seventh finding) (2026-07-21)
D-10 certified, as the precondition for the specialist's ruling-3 contamination certification, that the complete set of execution-1 human-visible quantities was the 240-byte verdict payload, and that "no PC1 TPR, CI, PC2, power, or denominator value has ever been surfaced to a human." That statement is false as written. phase2a_accounting_and_gates.json (2,726 B, mtime 2026-07-13, ~5 days before the execution-1 block run of 2026-07-18) contains PC1_TPR_at_b05, PC1_TPR_BCa95, and two TPR_minus_ref readings -- indicator x label quantities produced by gates.pc1_gate over the whole-L population.

This is a defect in an attestation rather than in code or an artifact. The no-peek corridor itself held: execution 1's block gate quantities were computed in memory and never written, and the 240-byte payload with decision_quantities = null remains a true record.

Scoping facts, stated without prejudging the re-certification: the quantity is over whole-L (all controls, §4), not the Study-2 CLEAN analysis population; no purified-population indicator x label quantity has ever existed; the value is already anchored and public in D-2 and is the published headline of the Study 1 pathology finding; it was computed under gates.py's "§7/§11 validity gates -- non-decision quantities, authorized to run pre-ratification" header.

Consequence: the ruling-3 certification was granted against an enumeration now known to be incomplete and must be re-granted by the certifying party against a corrected and complete one. The corrected enumeration is under construction by exhaustive sweep; D-10's language will be re-scoped from "no PC1 TPR ever surfaced" to the Study-2 clean-population block gate specifically, with all whole-L quantities enumerated alongside. The corridor remains shut pending re-certification. Logged before the sweep.

---

## D-29 — Contamination clearance RE-CERTIFIED against the corrected enumeration (2026-07-21)
The certifying party re-granted clearance on three independently sufficient legs: (1) category (b) is provably empty — no Study-2 CLEAN-population indicator×label quantity has ever been written, verified directly by exhaustive sweep including six external directories; (2) the sole whole-L PC1 TPR is over the wrong population to inform the clean analysis — the starvation-dominated high-miss regime it reflects is precisely what the CLEAN population excludes, so it bounds nothing about PC1 on 766 eval-clean controls at k = 38; (3) no post-hoc tuning path exists — the CLEAN population is registered at wka72 §2, frozen before the census, and the census is label-side only. Clearance now rests on a demonstrated empty category rather than an unchecked attestation.

## D-10.R — Re-scoped enumeration (approved with tightenings) (2026-07-21)
No purified-population indicator×label quantity has ever existed on disk — verified by exhaustive sweep including six external directories. The no-peek corridor held: the block emitted only {verdict, reason, payload_sha256} and decision_quantities = null. The sole whole-L PC1 TPR and BCa CI resides in phase2a_accounting_and_gates.json, computed 2026-07-13 under gates.py's authorized non-decision pre-ratification header. That quantity is over all controls, not clean controls — category (a) — and therefore cannot inform the clean analysis. It is already anchored and public as D-2, the Study 1 pathology headline.

## D-25.A — Disclosure updated to seven findings (2026-07-21)
Amend D-25 from four findings and two divergences to seven findings, four divergences, adding the attestation-defect category (D-28). Narrate neutrally. State the sequence, the catch, and the fix. Do not write any sentence claiming the corrected record strengthens the work — the record is the argument, and making the argument out loud weakens it. Delete any editorializing on rigor.

---

## C*** — anchor: closure of D-26 (fix) and D-27 (relabel), with the population-semantics assertions (2026-07-21)
Fresh code anchor **C\*\*\*** = `CODE_ANCHOR.sha256` rollup `42075400bf732fb011686ad549e5cec606f58af9d249fa2b6194053e8b7c3475`, superseding **C\*\*** (`8322f560`), which superseded **C\*** (`096355fe`). Lineage stated: C\* → C\*\* → C\*\*\*; instances are superseded with this versioned note, never re-argued. Anchored BEFORE the blind block and BEFORE any real-engine indicator×label quantity; `decision.RATIFIED = False` in anchored source.
- **D-26 fix** (`pre_census.py:51`): one line, `ev["controls"] → ev["clean_controls"]` — the §6 matched-count k table now over eval CLEAN controls (766-class), per wka72 §6.
- **D-27 relabel** (code only; artifact untouched, not re-run): `run_phase2a.py` and `gates.py` header notes state their gates and matched-count table are §4 WHOLE-L diagnostics, NOT the registered §5/§6 operating points, which are computed only in the block over CLEAN. Field `budgets_matched_counts_eval_controls → s4_wholeL_diagnostic_counts`. Grid decoupled from `C.BUDGETS` to the explicit §4 constant `{0.01,0.02,0.05,0.10}` (ruling 4). `population.py` / `population_accounting` unchanged (correct as §4 whole-L composition).
- **(B) PRIMARY assertion** (`tests_population_semantics.py`): no site OUTSIDE the block computes a clean-population gate / indicator×label quantity — the block (producer/block_orchestrator) is the only `matched_tpr` caller permitted to restrict to CLEAN; `gates.py`/`run_phase2a.py` remain whole-L. Structural + source-scan; breaks the build (negative control verified). This is the assertion whose failure would void the contamination clearance.
- **(A) SECONDARY assertion**: §6-denominator conformance scoped to the block + `pre_census` only; deliberately NOT applied to the whole-L diagnostics.
- **KNOWN BOUND** (honest, not closed): both catch regressions at enumerated/scanned sites; a newly-added, unregistered clean-gate site outside the block is not auto-caught until registered. (B) structural > (A); neither is a solved problem.

## D-27.C — code/artifact field-name divergence (D-18/19/20 shape), disclosed not discovered (2026-07-21)
The phase2a_accounting_and_gates.json artifact was written under pre-C\*\*\* code and carries the field name budgets_matched_counts_eval_controls. Under C\*\*\* the same computation is emitted as s4_wholeL_diagnostic_counts. Values are unchanged — the relabel altered no arithmetic, no grid and no population. Re-running under C\*\*\* would reproduce identical values under the new name. The artifact is retained unmodified as the published D-2 source.

## D-24.2 — Pre-census re-derivation under C***: expectation pre-committed (2026-07-21)
Logged BEFORE re-deriving. Under C\*\*\* the pre-census k table is over eval CLEAN controls (D-26). **Dual tripwire, pre-committed before any number exists:** (1) the k values WILL change — that is the fix; expected k is derived independently from the anchored per-arm eval-clean counts, then cross-checked. (2) the route_cap MUST hold at UNCONSTRAINED, identical to the C\*\* instance — the floor reads `ev["clean_controls"]` and `ev["events"]`, untouched by the k-denominator fix, so a changed cap is a **tripwire not a result** (it would prove the floor is entangled with the k denominator): HALT, D-entry, investigate. Derived layer only; no replay re-run.

---

## I-10.2 — Pre-census artifact supersession under C*** (2026-07-21)
C\*\* instance `8df3ff85cf2065e443dd42d5783366ebe9cd08ba1871f9911efee8ba42777c8e` (k over ALL controls) → C\*\*\* instance `1f2d3366499d11e44ba826803799d7642d8c0a35c11cc694e8c08403ebca1cf3` (k over eval CLEAN controls). Reason = **D-26**. **The ratified I-10 rule is UNCHANGED** (I-10.1 → I-10.2, versioned note only). Dual tripwire cleared: (1) k changed to the clean values as pre-committed — independently derived from the anchored per-arm eval-clean counts and cross-checked identical (popularity 379→{8,19,38,76}; ts_102 1086→{22,54,109,217}; ts_103→{22,55,109,219}; ts_104→{22,54,109,217}; ts_105→{22,56,112,223}; ts_106→{22,56,112,225}); (2) `route_cap = UNCONSTRAINED`, identical to the C\*\* instance — the I-5a floor reads `ev["clean_controls"]` and `ev["events"]`, untouched by the k-denominator fix; the cap did not change, and could not. Derived-layer only (`rederive_pre_census.py`); no replay re-run. C\*\* and C\* records retained under `PRE_CENSUS.sha256`. `decision.RATIFIED = False`.

---

## D-30 — Provenance closure: the execution-1 block runner committed after the fact (2026-07-21)
The D-23 sweep recorded that the runner which produced the (vacated) execution-1 verdict lived in the scratchpad, not the anchored tree, while `block_orchestrator.py` was anchored — the D-18/D-19/D-20 precedent class (an artifact whose generating path is uncommitted). The runner is now committed verbatim as `run_block_execution1.py`, **byte-equivalent** to what produced execution 1 (sha256 `1d5a86238546dbae3ed680f2be32b4204787f050383c4cabaa84bb4f050d93dc`): it sets `decision.RATIFIED = True` in-process (anchored source stays False), verifies the cap artifact against `CAP_SHA = 28a73b3a…` (the C\* pre-census that execution 1 used), and writes `s2_verdict_payload.json`. Because it pins the C\* cap hash, it **cannot** run under C\*\*\* (the live pre-census is `1f2d3366…`; `_verify_cap_artifact` would hard-fail) — a safety property: this is a provenance record, not the corridor runner. The execution-2 corridor runner is a separate, separately-authorized artifact pinning the C\*\*\* cap. No other artifact-generation path is uncommitted. `decision.RATIFIED = False` in anchored source.

---

## D-31 — Execution-2 corridor runner created and anchored (before any run) (2026-07-21)
`run_block_execution2.py` (sha256 fe07fe43ca178df3f53ded2e1b6d770195ac56d9ae6a50f1664fa785ab71fc2c) is the corridor runner for the registered analysis under C***. Structurally identical to `run_block_execution1.py` (same call path `compute_summaries → run_block`, same emission discipline — only {verdict, reason, payload_sha256} reach stdout; no decision quantity, gate value or count reaches stdout, the log, or any file other than the sealed gitignored payload). Diff confined to the pinned hashes: CAP_SHA `28a73b3a…→1f2d3366…` (the C*** pre-census) and an added CODE_ANCHOR pin `42075400…` (the C*** code anchor), plus docstring identity (execution 2 / C***). Flips `decision.RATIFIED` in-process only; anchored source stays False. The input-content and population-conformance assertions run at the top of `compute_summaries` (before any summary computation); the cap verification runs in `run_block` (after summaries, before the verdict/payload write) — on any mismatch `run_block` raises before any write, so no decision quantity reaches disk (no-peek preserved). COMMITTED AND ANCHORED; NOT RUN. The corridor opens only on separate explicit authorization. `decision.RATIFIED = False` in anchored source.

---

## S2-EXEC-2 VERDICT — the registered analysis, executed once under C*** (2026-07-21)
The corridor was opened once, uninterrupted, under `run_block_execution2.py` (C*** — cap `1f2d3366`, anchor `42075400`). This is the **first execution of the registered Study-2 analysis**. The input-content assertions (panel `ea972e2e`, ratings `360fb201`, covariate cache `c338f55b`), the population-conformance assertions (per-class per-fold set-hash vs census `6b6ac55`), and the cap verification all passed — the block reached a verdict rather than hard-stopping.

**VERDICT = SIGNAL DIES.** **REASON = "fails on own merits: c1=False c2=False c3=False"** (criteria 1–3 not met; §11 lattice route (ii)). The emission discipline held: only `{verdict, reason, payload_sha256}` left the block; no decision quantity, gate value, or count reached stdout, the log, or any file other than the sealed payload. The full payload — including `decision_quantities` — is written to the gitignored/embargoed `s2_verdict_payload.json` and was **not inspected**; it is hash-anchored: file sha256 `639b970cbc22945ca1ce99514b233e430aeacd4d6c3615171707012eba97a04a`, internal payload_sha256 `472571471d94ddbc69e634bec4c1fdb21b531148fdebd5fbff7ab655a03b3685`, `S2_VERDICT.sha256` rollup `f1f26e74…` (commit `cb7d954`, post-commit re-verified). Supersedes the vacated execution-1 record (D-9; former `S2_VERDICT` fe6af9ed / bfc7e0db, `decision_quantities=null`). `decision.RATIFIED = False` in anchored source throughout (flipped in-process only). Per the publication commitment (§12), the SURVIVES/DIES route publishes the full pre-registered analysis as the verdict.

---

## D-32 — Thesis correction, three edits (2026-07-21)
**(a) Withdrawal.** "Post-purification unlearnability" is withdrawn root and branch from every draft, log summary and outreach brief. It was an artifact of the vacated nonconforming execution.

**(b) Corrected construct claim.** "Construct = engine-frontier mechanics" overstates and is withdrawn. The accurate finding: the original label conflated a real, learnable user-process signal with engine-frontier mechanics. The contamination and the random-arm degeneracy demonstrate the mechanics component; the passed PC1 gate demonstrates the recovered user-process component. Purification separated the two rather than revealing emptiness.

**(c) Scoped §11 claim.** Validated as exercised: competing-risk purification, the shadow-class census, and the learnability gate — sufficient for measurability on this dataset. Still proposed: the replenishing frontier and engineered prevalence — necessity partially evidenced, sufficiency untested.

**(d) Negative's claim ceiling.** The pre-registered composite S = z(G) + z(p) − z(δ) showed no advantage over miss-based baselines at matched budgets, no consistent sign across budgets, and no incremental discrimination over covariates — on this benchmark, this engine family, this horizon. This kills the registered hypothesis. It does not establish that no slate feature could ever work. Paper sentence: whatever early-warning information exists in this system lives in the outcome stream, not in slate structure — and the theory predicted the opposite.

## D-33 — Phase 4 scope, CLOSED, fixed blind to all values (2026-07-21)
REQUIRED: bounding diagnostic; t_split {15, 25} sensitivity in full primary-analysis form (not gate-only — that spec was route-conditioned on UNINTERPRETABLE and is void); frontier-headroom split; indicator correlation matrix; I-5b absolute-discrimination reporting on the existing control arms; starvation-concurrence probe.

MOOT, with published reasons: MF/sequential engines ("existed to test mechanism specificity of a detected signal; none was detected; not run" — DEFERRED as future characterization); random re-run (the degeneracy is the result); H3 as frozen (vacuous by population fact — 3 within-window starvers; numbers reported).

This list is closed. Anything not on it is a D-entry proposal that defaults to denied.

## D-34 — Bounding-diagnostic rationale correction, logged before it runs (2026-07-21)
The prior anti-resurrection argument cited post-purification unlearnability and is void. The replacement is stronger: d_purified is a descriptive contrast computed on the same population where the pre-registered, out-of-fold, matched-budget test has just returned DIES. No descriptive effect size outranks the registered test, by construction. Both directions publish; neither resurrects anything.

## D-35 — Post-verdict compute universe (2026-07-21)
The pre-registered set is the entire post-verdict compute universe. No new quantity may be computed from the unsealed values. Route B forbids all re-analysis: no threshold exploration, no alternative composites, no feature engineering against the purified label, no additional slate statistic. Every such impulse is a new registration under the reopening conditions, which are external-party conditions and have not fired. The §4.3 anomaly rule covers defects only — logged before investigation, validated on label-permuted synthetic data, both results published.

---

## D-36 — Intent to unseal (2026-07-21)
The seal protected pre-verdict corridor integrity. The verdict is anchored and immutable; §12 commands publication of the full pre-registered analysis; the quantities therefore open. Phase 4's scope (D-33) and the bounding rationale (D-34) were fixed and anchored before any value was visible (v38, commit `2c1601c`).

Re-cite §4.3 explicitly: the moment values become visible, the temptation surface returns. A ΔTPR near but below 0.05 is a null. The answer to that whisper is already written in Route B (D-35) and in D-34. No quantity outside the pre-registered set (D-33) is computed; any such impulse is a new registration under external-party reopening conditions that have not fired.

---

## D-45 — Cancellation arithmetic + VIFs, and a struck rationale (2026-07-22)
**Cancellation arithmetic (same permissible class as D-43 — algebra over anchored values, no data accessed).** §5.1 now reconciles the composite's near-chance performance with the published marginals directly at the effect-size level: the registered contrast combines the anchored purified marginals (item 1) as (−0.64) + (+0.05) − (−0.68) = **+0.09**, against the composite's standard deviation √(3 + 2[r(G,p) − r(G,δ) − r(p,δ)]) = √(3 + 2[−0.478 − 0.833 − (−0.415)]) = √1.208 = **1.10** (correlations from the anchored cal-clean matrix, D-44), giving a net standardized separation of approximately **0.09 / 1.10 ≈ 0.08**. Stated in the paper as approximate (treats marginal effect sizes as pooled-SD standardized). This is the honest reconciliation that replaces the withdrawn D-43 variance-decomposition: the near-chance result arises because the marginal effects of z(G) and −z(δ) nearly cancel (−0.64 + 0.68 = +0.04) and z(p) adds only +0.05, so the composite carries ~0.08 of standardized separation. Also logged: VIFs derived from the anchored cal-clean correlation matrix (inverse-diagonal) — approximately **3.5 (G), 3.3 (δ), 1.3 (p)**.

**Struck rationale.** Any explanation that resolved the near-identical evaluation-arm values r(G,p) = −0.8694 and r(p,δ) = −0.8689 by reasoning that "collinearity at 0.975 forces near-identical relations to p" is STRUCK: the near-identity was itself the contamination artifact, and on the correct standardization population the values are −0.478 and −0.415, which are not near-identical. A search of the log confirms no such rationale was ever recorded; the two values remain correctly reported as evaluation-arm figures (only the erroneous explanation is disavowed). `decision.RATIFIED = False`.

## D-44 — RULING + REMEDY: item-4 correlation recomputed on the standardization population (2026-07-22)
**Withdrawal.** The §5.1 variance decomposition (D-43) is WITHDRAWN as published: verification established the anchored item-4 correlation matrix was computed on the full evaluation-fold arm (n=12,606: events + clean + starved controls), not the 694 calibration-fold CLEAN controls where the z-parameters are defined. On the eval arm the z-scores do not have unit variance, so Var(z(G)−z(δ)) = 2(1−r) does not hold and the decomposition is invalid as stated.

**Ruling (adjudicating party).** The original Phase-4 scoping ruled the correlation matrix "REQUIRED — frozen §6 descriptive commitment, trivially cheap ON CALIBRATION-FOLD CLEAN CONTROLS, interpretive rule unchanged." This SPECIFIES the population, not merely the cost: a REQUIRED-item spec that names one specific population names it as the analysis population, and a §6 descriptive commitment about the standardized indicators is by construction on the standardization population (the 694 cal-fold CLEAN controls where z is defined). The executed full-eval-arm population — folding the event class and the contaminated starved class into a "correlation among indicators" — is not a defensible alternative §6 descriptive population; it is an implementation default (the code loaded wide). Therefore the executed population is a CONFORMANCE DEVIATION from the ruled scope, of the D-7 class (executed population broader than specified), and recomputation on the specified population is a REMEDY, not a new-computation request. Permitted.

**Scope of remedy.** Pearson correlations among G, p, δ on the 694 cal-fold CLEAN controls. Control-only; no label; no indicator×outcome pairing; cannot bear on the verdict or any criterion; not category-(b) (indicator×indicator, no label). On this population z has unit variance by construction, so the decomposition becomes exact.

**Pre-committed reading (fixed before any value exists).** Whatever r(G,δ) is, it publishes. Pre-committed line: "high/comparable to the eval-arm 0.975" ≡ r(G,δ) ≥ 0.90 (so Var(D) = 2(1−r) ≤ 0.20 and the near-cancellation dominates); "materially lower" ≡ r(G,δ) < 0.90.
- r(G,δ) ≥ 0.90 → the decomposition is restored to §5.1 with the correct population stated, and D-43 is reinstated with the corrected figures.
- r(G,δ) < 0.90 → the geometric-infeasibility / near-cancellation argument weakens or fails, §5.1 reverts to the qualitative form permanently, published as a limitation on the explanation offered for the null.
The DIES verdict is untouched either way. No other quantity is computed — no threshold, no composite, no label-side comparison. `decision.RATIFIED = False`.

**Result (computed AFTER this reading was anchored at v47 `047ea75`).** On the 694 cal-fold CLEAN controls: r(G,p)=−0.47798, **r(G,δ)=0.83298**, r(p,δ)=−0.41469. Decomposition on the correct population: Var(D)=2(1−0.83298)=0.33404; Cov(D,z(p))=−0.06329; Var(S)=1.20745; occupancy variance share=1/Var(S)=**82.8%**. r(G,δ)=0.833 < 0.90 → **REVERT**: the eval-arm 0.975 was inflated by the event + starved classes; on the standardization population the G−δ contrast does NOT nearly cancel (Var(D)=0.334, not 0.05). **§5.1 reverts to the qualitative form permanently; D-43 is NOT reinstated.** Occupancy still dominates (~83%) but the geometric-infeasibility / near-total-cancellation claim is withdrawn. Verdict untouched. Anchored in `s2_exec2_calclean_correlation.json`.

## D-43 — ANALYTICAL DECOMPOSITION ADDED AT REVISION, ruled permissible (2026-07-22)
Paper section 5.1 was revised to include a variance decomposition of the registered composite: S = D + z(p) with D = z(G) − z(delta); Var(D) = 2(1 − 0.9751) = 0.0498; Cov(D, z(p)) = r(G,p) − r(p,delta) = −0.8694 − (−0.8689) = −0.0005; hence Var(S) = 1.0488 and the occupancy term accounts for approximately 95% of the composite's variance. This is algebra over quantities already anchored in the Phase 4 item-4 correlation matrix. No new empirical quantity was computed and no data were accessed. Ruled permissible under D-35 by the adjudicating party as analytical interpretation rather than analysis, in the same category as observing that an absolute TPR of 0.089 exceeds a chance rate of 0.05. Logged for the record so that the decomposition's provenance — derived at revision, from anchored inputs — is explicit.

## D-42 — PERMITTED CHARACTERIZATION: history-richness of the starved stratum (2026-07-22)
Scope granted under D-33 (default-denied mechanism). Logged BEFORE any number exists; the sequence is provable and preserved.

**Scope.** Descriptive comparison of the anchored I-1 covariate `log_activity = log(1 + warm_start_positive_count)` between the 3,292 starved-at-≤20 controls and the 1,460 clean controls. Label-side and covariate-side ONLY: no indicator, no composite, no criterion, no threshold, no decision quantity, nothing derived from the unsealed payload. Both strata are census-defined; the starved stratum was excluded from the registered population entirely, so this computation cannot touch the verdict's inputs even in principle.

**Semantic precision (binding on all reporting).** `log_activity` measures WARM-START POSITIVE COUNT — thin early engaged-positive history — NOT total rating volume. I-1 lineage: `n_total_ratings` was rejected because total ≈ warm-start + frontier + future, making it frontier-coupled and the comparison near-tautological (few future positives → small frontier → starvation, by definition). Warm-start positivity is the pre-treatment, anti-circular operationalization, which is what makes this an empirical question rather than an identity. **Licensed phrase: "users with thin early positive history." FORBIDDEN: "users who rarely used the service"** — it claims what was not measured.

**Pre-registered direction and trigger, fixed before any number exists.** Predicted direction: starved BELOW clean. Direction is pre-registered because the mechanism admits a non-monotone alternative — frontiers also deplete through hits — so a null or reversed result is conceivable and would be genuinely interesting. Form: median and IQR per stratum; standardized difference on the log scale with 10,000-iteration bootstrap CI, seed 201 (registered lineage); ECDFs published. **"ASSOCIATED" is declared if and only if ALL THREE hold:** difference in the predicted direction (starved below clean), |d| ≥ 0.2, and CI excluding 0. Any other outcome — null, reversed, or sub-threshold — publishes identically and the plain-language summary continues its omission. No adjudication after seeing shapes.

**Foreclosed readings, both directions, fixed now.** (a) "The benchmark was confounded by history richness, therefore the null is uninformative." Foreclosed: the registered analysis ran on the purified population from which starved units were excluded entirely; an association among EXCLUDED units cannot bear on a comparison that did not include them. (b) "The null holds only for history-rich users, so the signal may live among the sparse." Foreclosed: the starved-sparse stratum is unlabelable BY CONSTRUCTION in this benchmark family — nothing about S was measured on them and nothing could be, under any design in this family. This is not a qualification of the DIES verdict; it is the §11 scope statement the paper already makes. This comparison can make that scope more legible, never smaller.

**Non-inference clause (required).** `pop_affinity` was anchored and available and was DELIBERATELY NOT COMPARED. One pre-motivated question, one variable. Taste-popularity is a different hypothesis, not tested, and no inference about it is licensed. Any further characterization of the excluded stratum is a new proposal, default denied. This clause prevents silent covariate-mining in one direction and a reader inferring an exhaustive check that never happened in the other.

**Result (computed AFTER this reading was anchored at v44 `6550ccd`).** Set-hash assert PASS (starved 3,292 / clean 1,460, match census). Median log_activity: starved 2.890 (IQR 2.639–3.091), clean 3.045 (IQR 2.833–3.219). Standardized difference (starved−clean) Cohen's d = **−0.441**, CI [−0.500, −0.381] (10k, seed 201). **ASSOCIATED: FIRED** (all three: direction starved-below-clean ✓, |d|≥0.2 ✓, CI excludes 0 ✓). Licensed reading: users with thin early positive history are over-represented in the starved stratum; foreclosed readings (a)/(b) and the non-inference clause bind. Anchored in `s2_exec2_starved_history.json`.

**Compute discipline.** Source: the C\* covariate cache, decompressed `b7a0aa36…` — the 25,020-row superset, the only anchored artifact retaining the starved rows. BEFORE computation, assert row extraction by user_id set-hash against the anchored census `6b6ac55`: starved-at-≤20 = controls where NOT `is_candidate` (expect 3,292); clean = controls where `is_candidate` AND NOT `starved_in_2050` (expect 1,460). Any mismatch halts. Compute exactly the form specified — nothing else; no `pop_affinity` for either stratum; no additional statistic, split, or subgroup. `decision.RATIFIED = False`.

## D-41 — Item-6 label correction: two distinct control populations, not one (2026-07-22)
The initial item-6 report conflated two DIFFERENT populations under one "within-window starver" figure. They are distinct and the item is split into two rows (a labeling/reporting split over populations that already exist in the run — no new computation; the census 6b6ac55 defines them):
- **(a) H3-as-frozen** — STARVED-in-(20,50] (candidate at landmark, starved post-landmark): **literally 3 units** vs CLEAN (1,460). VACUOUS by population fact — report the counts and the vacuity; NO discrimination computed.
- **(b) starvation-concurrence probe** — STARVED-at-≤20 (pre-landmark, excluded from L₂): **3,292 units** vs CLEAN (1,460), indicators over steps 1–20. The substantive probe.
**Partition check:** 3,292 (pre-landmark) + 1,460 (clean) + 3 (post-landmark) = 4,755 total controls — must close.
**Pre-committed reading for (b), fixed before the number:** if the slate indicators (z(G), z(p), z(δ)) and composite S separate the 3,292 pre-landmark-starved units from the 1,460 clean controls with large effect (|Cohen's d| ≥ 0.8), then slate structure reads frontier depletion — S is in part a frontier-depletion detector, which is exactly the contamination the CLEAN-population purification removes (and explains item 1's whole-L→purified d collapse). |d| < 0.2 would mean slate structure does not encode pre-landmark starvation. Both directions are descriptive; neither disturbs the anchored DIES verdict (D-34 / Route B). `decision.RATIFIED = False`.

## D-40 — Phase-4 execution complete (post-verdict, SIGNAL DIES route) (2026-07-22)
The pre-registered post-verdict compute universe (D-33) is executed. Anchored two-tier as `S2_PUBLICATION.sha256` (rollup `f640d37d…`): §12 publication tables, the re-derived PC1 gate + per-detector operating tables (value-matched to the sealed payload), the D-37 power probe, and the six Phase-4 items + not-run ledger.
- **Known-answer gate:** item-2 t=20 reproduces the sealed payload's ΔTPR-by-budget exactly (points + full BCa CI @10k) — the harness faithfully reproduces the registered analysis.
- **PC1 learnability gate PASSED** on CLEAN: TPR@0.05 = 0.561 [0.549, 0.574] (D-32b, recovered user-process component). **S is near-chance** at every budget (TPR ≈ 0.047/0.089/0.148/0.240), strictly dominated by PC1 and the miss-based families → ΔTPR < 0 everywhere.
- **Bounding diagnostic:** the slate indicators' whole-L discrimination is contamination — d collapses/reverses on CLEAN (z(G) d_full 2.01 → d_purified −0.64; z(δ) 2.00 → −0.68; z(p) −2.25 → +0.05); only miss_run_fraction retains on CLEAN (d_purified +0.81). Mechanical Δd CI excludes 0 for all four.
- **t_split sensitivity:** robust null at 15/20/25 (c1=c2=False at all; c3=False at 20 and 25; c3 COVERAGE-LIMITED at 15 per D-38, 58.65% covered).
- **Frontier-headroom:** low-headroom bin [10,15] has 38 events (<100, not powered, reported); >15 bin ΔTPR@0.05 = −0.470 = primary.
- **Correlation:** indicators near-collinear (|r| 0.87–0.98), one miss/frontier axis.
- **I-5b:** S beats chance on 0 of 6 severed arms.
- **Starvation-concurrence / H3:** 0 events cross the floor before collapse; within-window starvers = **3,292** (all controls, 0 events; reconciles with the 3,292 pre-landmark-starved L₂ exclusions — the ledger's "3" read as shorthand for 3,292). H3 vacuous on the event class by construction.
- **Not-run ledger:** MF/sequential, random re-run, H3-as-frozen — three MOOT with published reasons.
Generation paths committed before execution: `phase4_execution.py` (`48706a0`), `rederive_publication_tables.py` (`6f8bfe2`), `tsplit_power_probe.py`. `decision.RATIFIED = False` (anchored source + module value); C*** `42075400…` unchanged.

## D-38 — t_split=15 covariate coverage, pre-committed before any value (2026-07-21)
D-33 specified the t_split {15, 25} sensitivity in FULL primary-analysis form. The §9 covariate cache (21,725 rows, built over the t=20 CLEAN population; pop_affinity LOUO) does not cover the additional events admitted when the landmark moves in to t=15 (events collapsing in (15, 20]). Declared now, before any value is computed — **which form each landmark receives:**
- **t_split = 25 — FULL form.** Its population (events collapsing in (25, 50]) is a strict subset of the t=20 population and is fully cache-covered; c1, c2, c3 and both gates all compute.
- **t_split = 15 — PARTIAL form (criterion 3 COVERAGE-LIMITED).** c1, c2, and the PC1/PC2 gates compute fully (they are covariate-free). Criterion 3 is reported COVERAGE-LIMITED, with the covered / uncovered eval-event counts published and the reason stated. **The covariate cache is NOT extended and no population is adjusted to make c3 computable** — recomputing covariates for the extra users would be new computation outside the pre-registered set (D-35 / Route B). A registered analysis whose covariate model does not cover an alternative landmark's expanded population is a reportable scope fact, stated, not engineered around. `decision.RATIFIED = False`.

## D-39 — Phase-4 harness mechanics, disclosed before the run (2026-07-21)
`phase4_execution.py` was authored post-verdict (D-18/19/20 precedent class) and is anchored BEFORE the real run, not after — every artifact it produces will be published, so its generation path is committed first. Mechanics recorded plainly: (a) it sets `decision.RATIFIED = True` **in-process only**; the anchored source remains `False` and C*** (`42075400…`) is unchanged. (b) Item 2 uses a **contained monkeypatch** of `config.WINDOW`/`config.T_SPLIT` to vary the landmark, applied and restored **within item 2 alone**; all other items run at the registered t=20. (c) Item 3 contained a **placeholder defect** in its first draft (it reported an S-only TPR rather than the matched ΔTPR against the registered baseline matrix); the defect was found and corrected before execution, and item 3 now computes the matched ΔTPR via the registered `delta_tpr_ci`. (d) Item 2 carries a **t=20 known-answer arm**: the identical code path must reproduce the sealed payload's ΔTPR-by-budget (points always; full BCa CI at 10k), asserted — a mismatch HALTS the run. `decision.RATIFIED = False`.

## D-37 — t_split sensitivity power handling, pre-committed before the counts are known (2026-07-21)
The t_split ∈ {15, 25} sensitivity analyses (D-33 REQUIRED) are run in full primary-analysis form over populations re-derived at those landmarks. Registered rule, fixed before the complete-window populations are counted: **if the complete-window eval-fold EVENT count at t_split = 15 or t_split = 25 falls below the §11 power floor of 200, that landmark's sensitivity analysis is reported NOT EXECUTABLE** — the count is published, the reason is stated, and the analysis is not run. No window, filter, floor, or admission rule is adjusted to make an underpowered landmark run. A registered sensitivity analysis that cannot be powered on the available data is a reportable fact, not a defect to engineer around (the §11 power gate governs a sensitivity landmark identically to the primary). Logged before the counts are computed. `decision.RATIFIED = False`.

---

## Verified conformance (no deviation)
**[Scope note — D-27 relabel logic, added post-verdict.** The figures in this paragraph are **§4 WHOLE-L validity accounting** over ALL controls on the Study-1 budget grid — the panel-integrity, fold-balance and power checks. They are **NOT** the registered §6 operating points. Per **D-7** and **D-15**, the Study-2 §6 analysis population is the **1,460 CLEAN controls (766 eval; matched counts round(b · 766) = {15, 38, 77, 153})**, not the 4,755 whole-L controls / 2,453 eval controls / {25, 49, 123, 245} shown here. Both are correct in their own scope; only their co-location under one heading could read as a contradiction. "L = 25,020" and "round(b · 2,453)" are whole-L §4 figures — consistent with, not contradicted by, the §6 divergences logged in D-7/D-15.]
Panel 1,093,072 rows × 24 cols; `slate_json` length = 10 with zero exceptions; every L unit has a complete steps 1–20 window; whole-horizon labels match the frozen counts (35,584 event / 4,755 control); early degraders excluded from L = 15,319; **L = 25,020 (20,265 events + 4,755 controls)**; §5 fold split ~50/50 (eval events 10,153; cal controls 2,302; eval controls 2,453); **§11 power gate satisfied (10,153 ≫ 200)**; §8 matched counts round(b · 2,453) = {25, 49, 123, 245}; all six baseline families present; `warm_start_ratings = 30` confirmed constant (prefix-length covariate correctly dropped).
